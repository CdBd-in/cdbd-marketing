---
figma: https://www.figma.com/design/qRFc2HpQ4Mp562LQMB8BX0/블로그-이미지?node-id=1-10525
figma_page: 컴포넌트
page_id: 1:10525
updated: 2026-06-30
---

# 이미지 제작 가이드 — 자산·캡처

> 신규 템플릿(`377:5848`)을 **clone(복붙)** 해서 쓰므로, **폰/노트북 베젤·BG·그리드·화살표·데코는 템플릿에 이미 들어 있다** — 따로 instance로 만들지 않는다.
> 이 문서의 역할 = **템플릿의 교체 레이어(화면 스크린샷 rect·3D 오브젝트 instance·에디터 컴포넌트)에 넣을 픽셀을 어디서·어떻게 가져오나.**
> ⬆️ 템플릿·배치 → [[1-2-1-1. 공통]] · **캡션→빌드플랜·트랙 §5–§6** · 카탈로그 → [[1-1-3. 이미지 규칙|1-3. 이미지 규칙]]

---

## 0. 무엇을 직접 가져오나

> 템플릿 clone 후 **교체 레이어**([[1-2-1-1. 공통]] §2의 3종)에 넣을 픽셀만 이 문서에서 조달한다. 레이어는 **타입+위치로 식별**(의미론적 슬롯명 없음).

| 교체 레이어 | 넣을 것 | 방법 (이 문서) |
|---|---|---|
| **화면 스크린샷 rect** (폰/데스크탑) | CdBd viewer 페이지 캡처 | **§2 viewer 캡처** |
| **에디터·통계 화면 영역** | 에디터 UI — ① 컴포넌트 재사용 / ② 직접 캡처 | **§1 에디터 자산** · **§3 에디터 캡처** |
| **3D 오브젝트 instance** · 자유합성 | 3D 아이콘(Thiings)·실사진 | **§4 Thiings·실사** |
| **텍스트 노드** | 라벨·캡션 문구 | [[1-2-2-3. 텍스트 작성 가이드]] |
| 폰 베젤 · BG · 그리드 · 화살표 · 데코 | **템플릿 내장 → 안 건드림** | — |

---

## 1. 에디터 화면 자산 (에디터·통계 화면 영역에) — `1:10970`

> **② 강조·⑤ 프로세스** 등 에디터 화면이 필요한 자리에 넣을 **CdBd 에디터 UI**. 새로 캡처하기 전에 **이미 컴포넌트화된 아래 화면을 먼저** 쓴다. (없거나 다른 화면이 필요 → §3 캡처)

| 이름 | 노드 ID | 크기(px) | 무엇을 보여주나 | 라벨 예 |
|------|---------|---------|------------------|---------|
| **통계** | `1:11478` | 766 × 1003 | THE SIGNATURE DAY — 참여자 80명, 파이 차트, 답변 목록 | "행사 참여 통계 한눈에" |
| **예약카드** | `1:11479` | 400 × 360 | 예약 옵션 설정 다이얼로그 (날짜·시간·인원) | "예약 카드로 RSVP 받기" |
| **질문과 답변** | `1:11480` | 355 × 640 | 객관식·주관식 옵션 추가 패널 | "질문·답변 카드 빌더" |
| **카드 디자인** | `1:11481` | 470 × 712 | 카드 디자인 패널 (배경·테두리·모서리·여백) | "디자인 자유도" |
| **개인화 데이터** | `1:11482` | 556 × 330 | 데이터 추가/다운로드 테이블 | "CRM·개인화 발송" |
| **페이지 통계** | `159:6406` | 1920 × 1336 | 통계 요약(조회수·클릭수·클릭률+라인차트) + 링크별 클릭 TOP + 제출답변 — 2종→1종 통합(2026-06-24) | "페이지 성과 추적" |

**에디터 화면 영역에 넣는 법**
- 통계/대시보드는 **그대로 ❌** → 핵심 지표 카드를 **크롭·확대·중첩 강조**(② 강조 유형, [[1-2-1-3. 강조]]).
- 에디터는 **풀 3분할 덤프 ❌** → 보여줄 **의미 영역(특정 패널·카드)** 만 크롭 ([[1-2-3. 검수 체크리스트]]).
- 컴포넌트는 `createInstance()` → 강조·프로세스 콘텐츠 영역 위에 배치. **`rescale()` 주의** — 컴포넌트 텍스트·숫자가 깨지면 **원본 크기**로.

**어떤 에디터를 언제?** (본문 키워드 → 컴포넌트)

| 키워드 | 추천 컴포넌트 |
|--------|---------------|
| "RSVP" · "예약" · "참여 신청" | 예약카드 + 통계 |
| "설문" · "응답" · "질문" | 질문과 답변 + 통계 |
| "디자인 자유도" · "커스터마이즈" · "꾸미기" | 카드 디자인 |
| "CRM" · "개인화" · "엑셀 업로드" · "이메일 발송" | 개인화 데이터 |
| "지표" · "성과" · "트래픽" · "조회수" | 페이지 통계 |
| "행사 참여" · "결과" · "후기" | 통계 |

---

## 2. viewer 캡처 (폰 화면 rect에 넣는 것) ⚡

> 본문이 특정 viewer 페이지(프로필 링크·예약·룩북·초대장·카탈로그 등)를 보여줄 때 → 폰 화면 rect에 채운다. **viewer는 공개 페이지라 로그인 불필요.**
> 슬러그 카탈로그(17개) → [[1-1-3. 이미지 규칙|1-3. 이미지 규칙]] §2.

### 2.1. 🔴 절대 원칙 — 사람처럼 분석 후 섹션 분할 (멀티 폰: 기본 3폰·나열 그리드·프로세스 스텝)

> **여러 폰 유형은 같은 캡처를 복붙하지 않는다.** viewer를 사람처럼 읽어 의미 섹션을 식별하고, **각 폰에 다른 섹션**을 넣는다.

- viewer는 자연스러운 사용자 여정(소개 → 정보 → 액션)을 가짐 → 그 흐름을 폰 시리즈로 시각화.
- **모범 `1:12129` (Heritage Summit)**: 폰1 인사말 / 폰2 RSVP 폼 / 폰3 문의처.
- 폰 개수만큼 의미 섹션 선정 (기본 3단=3 · 나열 그리드=4 · 프로세스 스텝=3~4) → 각 화면 비율(폰 ≈ **0.464**)로 crop → 업로드 → 화면 rect에 IMAGE fill.

### 2.2. 캡처 인프라 — Playwright (권장 1순위)

**스크립트**: `자동화 도구/cdbd-capture/capture-seminar-3sections.mjs`
```bash
cd "자동화 도구/cdbd-capture"
npm install && npx playwright install chromium     # 첫 1회
node capture-seminar-3sections.mjs <viewer-url> <prefix>   # 예: .../invitation/seminar/viewer seminar
```
- 진입 → **풀스크롤로 lazy-load 모두 트리거** → `SECTION_HINTS`로 의미 섹션 분기 → 슬롯 aspect(390×838=0.4654)로 3장 캡처.
- 🔴 **hero `forceY: 0` 룰**: hero는 **항상 페이지 진짜 상단(y=0)** 에서 캡처. (키워드 매칭은 본문 중간에 걸려 **로고·타이틀이 잘림** — Heritage Summit 사고). 새 viewer 스크립트도 `{ name:'1-hero', forceY:0 }` 동일 적용.
- Fallback 도구(인프라 불가 시): thum.io `fullpage` / Microlink — **lazy-load 누락 잦음**.

### 2.3. 업로드 전 체크 + Fallback

**업로드 전 필수 (같은 화면 3개 들어가는 사고 방지)**
- [ ] crop 3~4장을 **나란히 Read로 비교** — 정말 다른 내용인가.
- [ ] 각 crop **핵심 1줄 요약** — 같은 요약 나오면 ❌ (예: 폰1 로고+타이틀 / 폰2 RSVP폼 / 폰3 연락처).
- [ ] **콘텐츠 영역 밖 crop ❌** — 빈 BG만 나오는 y 범위 금지 (lazy-load 미스 인지).

**distinct 섹션이 안 잡힐 때 (순서대로)**
1. **다른 viewer 슬러그** 시도 — `personalized`↔`seminar`↔`rsvp` / `oak_table`↔`lookbook`↔`newarrival`.
2. thum.io `wait/30+` 로 더 대기.
3. 같은 viewer를 **다른 스크롤 위치**(상단/중간/하단)로 — 단, 간격을 충분히 벌려 시각이 다르게.
4. 1폰=full hero / 나머지=브랜드 컬러 BG 데코.

---

## 3. 에디터 화면 캡처 (새 캡처) 🆕

> §1 컴포넌트로 안 되는 **새 에디터/관리/통계 화면**이 필요할 때. 에디터는 **로그인(admin)** 이 필요해 viewer 캡처와 절차가 다르다.

**언제**: ② 강조·⑤ 프로세스에서 §1 표에 없는 화면(특정 빌더·설정·CRM·통계 변형).

**방법 (우선순위)**
1. **컴포넌트 먼저** — §1 표에 있으면 캡처하지 말고 그 컴포넌트 instance 사용 (가장 안정적·일관).
2. **로그인 캡처** — Playwright로 CdBd 관리자(admin) 로그인 → 해당 빌더/통계 화면 이동 → 스크린샷.
   - 인프라는 viewer용(`자동화 도구/cdbd-capture/`)을 **로그인 스텝만 추가**해 재사용. (viewer=공개라 로그인 X / 에디터=로그인 필요)
3. **의미 영역만 크롭** 🔴 — 풀 에디터 화면 통째 ❌ → 보여줄 **패널·카드·통계 카드 1개**로 크롭 ([[1-2-3. 검수 체크리스트]]).
4. 업로드(`upload_assets`) → `imageHash` → 강조/프로세스 템플릿의 화면 영역에 fill. 통계면 핵심지표 확대 강조([[1-2-1-3. 강조]]).

> ⚠️ **에디터 로그인 자동 캡처 스크립트는 아직 없음** — 현재는 **수동 로그인 캡처 또는 §1 컴포넌트 재사용**이 기본. (→ TODO)

---

## 4. 3D 오브젝트·실사

### 4.1. Thiings 3D 아이콘 ✨

> 추상 개념(보안·성장·비용·문서·AI 등)을 상징할 때. **③ 비교·④ 나열·⑤ 프로세스**의 3D 오브젝트 instance에 사용.
> 카탈로그(슬러그·톤 매트릭스) → [[1-1-3. 이미지 규칙|1-3. 이미지 규칙]] §4.

| 본문 톤 | Thiings 슬러그 | 컴포넌트 ID(썸네일) |
|---------|---------------|------------------|
| 의료·B2B 의료 | `stethoscope` | `378:4` |
| 문서·계약·약관·PDF | `document` | `443:4` |
| 종이·인쇄물·아날로그 | `paper` | `443:7` |
| 파일·다운로드·자료 | `file` | `443:10` |
| 보관·정리·자료실 | `folder` | `443:13` |
| 매거진·매뉴얼·가이드북 | `book` | `443:16` |
| 명함·연락처 stack | `name-card-stack` | `491:2` |
| AI·자동화 | `magic-wand` | `4:167` |
| 일반 강조(데코) | `sparkle2/3/4` | `4:166/165/164` |

- 받는 법: `thiings.co/things/{slug}` → PNG → `upload_assets` → 슬롯에 배치.
- ⚠️ 블로그 이미지 파일엔 아직 Thiings 컴포넌트 미등록 → 첫 사용 시 썸네일에서 동일 PNG 가져와 등록 (TODO).

### 4.2. 실사진·외부 캡처 (합성 불가 트랙)

- **실사진**: 합성 불가 → 아카이브 소싱/촬영. 캔버스에 사진 배치(라운드 40). 주로 현장·후기·실사례를 보여주는 캡션.
- **외부 캡처**(경쟁사·툴·인스타): Playwright로 외부 URL 캡처(viewer 인프라에 URL만 교체) → 카드로 배치. ⚠️ 상표·저작권은 비교·인용 목적 한정.
- 트랙 분기 → §6.

### 4.3. 자산 관리 원칙

- ✅ 반복 쓰는 자산(Thiings·viewer 캡처)은 **imageHash를 [[1-1-3. 이미지 규칙|1-3. 이미지 규칙]]와 공유** — 이중 관리 ❌.
- ✅ 어느 글에 어느 자산을 썼는지 사용 이력 기록.

---

## 5. 캡션 → 빌드 플랜 (목적·템플릿·트랙) ⭐

> 🔑 **본문에는 이미지 자리마다 `*캡션*` 마커가 이미 박혀 있다.** (`*…*` 줄 = 이미지 위치 + 그 자리에 들어갈 이미지의 내용.) 그래서 "글을 분류해 몇 장 찍을지"를 추론하지 않는다 — **캡션 1개 = 이미지 1장**으로 확정하고, 각 캡션을 읽어 **목적·템플릿·트랙만** 정한다.
> - 절차: ① 본문에서 `*캡션*` 자리 추출(= 이미지 위치·개수 확정) → ② 캡션 메시지 1줄 해석 → ③ **목적 1개**([[1-2-1-1. 공통]] §1 라우팅) → ④ 템플릿·트랙 → ⑤ 빌드 플랜(출력 스키마).
> - **캡션이 없는 자리엔 이미지를 만들지 않는다.** 위치·개수·삽입 여부는 글쓴이가 캡션으로 이미 결정했다.
> - 🔴 **같은 목적 2장+ 쓰면 자산 차별화** — 3D 오브젝트는 다른 아이콘, 목업은 다른 페이지/카탈로그. ([[1-2-3. 검수 체크리스트]])

### 5.1. 캡션 메시지 → 목적 → 템플릿 (자동 결정)

| 캡션이 말하는 것 | 목적 | 대표 템플릿 노드 | 트랙 |
|---|---|---|---|
| 결과 페이지/사례를 그냥 보여줌 · 통계·응답 화면 담백히 | **① 기본** | 3단 `347:2809` · 모바일+데스크탑 `358:2034` · 서비스페이지 `347:3656`/`347:3658` | 합성 |
| 특정 버튼·기능·데이터를 콕 집어 설명 | **② 강조** | 버튼 강조 `347:2957` · 개인화 데이터 `363:1541` | 합성 |
| 기존 vs CdBd / 비용 대비 | **③ 비교** | 키워드 하단 `367:2459` · 비용 저울 `371:2246` | 합성 |
| 유형·조건·카테고리·카드구성 나열 | **④ 나열** | 카테고라이징 `347:3473` · 그리드 `347:3547` | 합성 |
| 사용법·플로우(1→2→3, 트리) | **⑤ 프로세스** | 사용법 `347:3268` · 개인화 URL 트리 `347:4251` | 합성 |
| 현장 실사진 | (실사진) | — | 소싱 |
| 경쟁사·외부툴·인스타 캡처 | (외부캡처) | — | 캡처 |

- **viewer 슬러그 결정**: 본문 키워드 → **§2 viewer 캡처** / 슬러그 카탈로그(17개)는 [[1-1-3. 이미지 규칙|1-3. 이미지 규칙]] §2. 매칭 없으면 fallback(seminar/personalized/rsvp/oak_table…).
- **에디터 컴포넌트 결정**: **§1 에디터 자산** 키워드표.

### 5.2. 출력 스키마 (per 이미지)
```json
{
  "order": 1,
  "section": "도입부",
  "purpose": "기본",
  "size": "1920x1200",
  "templateNodeId": "347:2809",
  "captureSource": { "kind": "viewer", "ref": "invitation/seminar", "sections": 3 },
  "replace": { "screens": ["sec:hero","sec:rsvp","sec:contact"], "texts": [], "icons": [] },
  "track": "composite"
}
```
→ 글 1편 = 이 객체의 배열(캡션 순서 = order). **합성(composite)**은 곧장 fill 파이프라인(§1~§4), **캡처/소싱**은 사람·스크래퍼 큐로 분기(§6).

---

## 6. 제작 트랙 분류 ⭐

| 트랙 | 해당 목적 | 제작 방식 | 사람 개입 |
|---|---|---|---|
| **① 신규 템플릿 합성** | **① 기본 · ② 강조 · ③ 비교 · ④ 나열 · ⑤ 프로세스** | 「템플릿」 `377:5848` clone → 교체 레이어 fill ([[1-2-1-1. 공통]] §2) | 자동 |
| **② 캡처** | 외부 화면·경쟁사·인스타·구글 | Playwright 헤드리스 캡처 (§2 인프라 재사용) | 반자동 (URL 지정) |
| **③ 소싱/촬영** | 브랜드 실사진 | 촬영 사진 아카이브에서 선택 (§4.2) | 수동 |

> 핵심: "글 던지면 전부 자동"은 불가. **실사진·외부캡처는 자동 생성 불가** → 빌드 플랜에서 트랙을 표시해 캡처/소싱 단계로 분기.

---

## TODO

- [ ] **에디터 로그인 캡처 스크립트** (admin) — viewer 캡처 인프라(`cdbd-capture`)에 로그인 스텝 추가 (§3).
- [ ] Thiings 3D 자산을 블로그 이미지 Figma 파일에 컴포넌트로 등록 (§4.1 9종) → §4.1 표에 본 파일 노드 ID 컬럼 추가.
- [ ] 새 viewer·새 에디터 캡처 발생 시 [[1-1-3. 이미지 규칙|1-3. 이미지 규칙]]에 먼저 추가 → 블로그 이미지엔 imageHash만 공유.
