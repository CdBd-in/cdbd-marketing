---
figma: https://www.figma.com/design/3LPfZ6a6vlJrZ4TXorVWJL/CdBd-블로그-썸네일
updated: 2026-06-19
---

# 이미지 규칙 (이미지 요소 & 조합)

> 어떤 순서로 만들고(§0), 무엇을 준비하고(§1), 몇 개를 어떻게 조합하는지(§2).
> 레이아웃은 → [[1-2-1. 레이아웃 규칙]]

---

## 0. 작업 순서 — 다중 유형 비교 (4-6안) 🔴 (2026-06-12 갱신)

> **핵심 원칙 (2026-06-12 갱신)**: 타이틀 1개 → **이미지 유형 2-3개 × 각 유형별 2안 = 4-6안** 자동 제작 → 사용자가 유형 자체부터 비교 선택.
> 단일 유형 결정 ❌ / **다중 유형 + 변형 매트릭스** ⭕.
> 전체 파이프라인(intake → 검수) → [[1. 블로그/썸네일/0. 목표와 비전]] §⚙️

| 순서  | 단계 | 내용 | 출처 |
| --- | -- | --- | -- |
| 1 | **이미지 유형 2-3개 결정 (A~E)** | 신호어 매칭 점수로 **1·2순위 유형 필수 + 3순위 선택** | [[1-2-1. 레이아웃 규칙]] §1.1 |
| 2 | **각 유형별 자산 1개 추출** | 1a 기능명·1b 산업·1d 톤 다층 분석 → 유형별 1순위 자산 | [[1-3-2. 이미지 출처]] §1 Step 2 |
| 3 | **자산 수집** (캡처·다운로드) | 유형별 viewer/에디터/Thiings/외부 자료 확보 | [[1-3-2. 이미지 출처]] §2·§3·§4 |
| 4 | **4-6안 자동 제작** (유형 × 2안) | **A·B·D**: 상단·서브 + 하단·서브 = 2안 / **C·E**: 다른 자산 조합 2안 / 각 안에서 슬롯 clone → 텍스트·이미지 fill | [[1-2-1. 레이아웃 규칙]] §2 + §1.1.b |
| 5 | **검수 + 선택** | 4-6안 중 사용자가 1개 선택 — **유형 자체부터 비교 가능** | §2.2 + 휴먼 검수 |

> ✅ **슬롯 시스템 효과**: 좌표·크기·여백·정렬·줄 수 위치 보정이 자동 → 자산만 바꿔도 일관된 베리에이션 자동 생성
> 📐 줄나누기·강조색 규칙 → [[1. 블로그/썸네일/1. 디자인 가이드/1-1. 스타일]] "텍스트 작성 규칙"
> 🎯 **베리에이션 메인 축**: 이미지 자산 (다른 viewer 캡처·다른 템플릿). 슬롯 위치는 보조 축.
> 🎯 "왜 이미지부터?": 이미지 유형이 정해지면 슬롯 종류가 결정되고, 슬롯이 결정되면 타이틀 자리도 결정됨. 반대 순서는 슬롯 시스템에서 불가능.

---

## 1. 이미지 요소 유형 — 무엇을 준비하나

이미지는 **역할**에 따라 두 가지로 나뉜다.

### 1.1. 주 이미지 (Main) — 5유형 (A~E)

콘텐츠 핵심을 시각화하는 요소. **시선의 초점**.
유형 결정은 → [[1-2-1. 레이아웃 규칙]] §1.1 (타이틀 → 유형 판단). 여기서는 각 유형의 **이미지 자산 준비·배치 규칙**만 정의.

| 유형              | 자산 구성                                           | 자산 출처                                                                          | 핵심 크기·배치                                                   | 슬롯                                                        |
| --------------- | ----------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------- | --------------------------------------------------------- |
| **A. 목업**       | 공식 목업 컴포넌트 1개 + viewer 캡처(의미 영역 크롭 §1.1.b) fill | 컴포넌트: §1.1.a / viewer: [[1-3-2. 이미지 출처]] §2 (템플릿) · §3.1 (viewer 기능)        | `x365.5 / y86 / w192 / h404.35` — **우측 하단 블리드** (cut-off, 상단/하단 통일 — 2026-06-12)           | [[1-2-1#2.1\|목업 슬롯 4종]] (`VISUAL_SLOT`)                   |
| **B. 목업 + 에디터** | 공식 목업 1개(축소) + 에디터 UI 캡처 1개 → **한 덩어리(주 1개)**   | 컴포넌트: §1.1.a / 에디터: [[1-3-2. 이미지 출처]] §3.2 (에디터 기능)                         | 목업 `w≈176 / h≈360` 우측 블리드 + 에디터 `260×154` contain (좌중앙·상단) | [[1-2-1#2.1\|목업+에디터 슬롯 4종]] (`VISUAL_SLOT`·`EDITOR_SLOT`) |
| **C. 멀티 목업**    | 공식 목업 컴포넌트 2~3개 (다른 카테고리·시리즈) → **한 덩어리(주 1개)** | 컴포넌트: §1.1.a / viewer: [[1-3-2. 이미지 출처]] §2                                 | 각 `w≈143 / h≈340` 가로 배열(엇갈림), 하단 블리드                       | [[1-2-1#2.1\|멀티 슬롯 2종]] (`VISUAL_SLOT_1/2/3`)             |
| **D. 3D 아이콘**   | 개념을 상징하는 3D 렌더링 오브젝트 1개                         | [Thiings](https://www.thiings.co/things) PNG 다운로드 → Figma 업로드 후 컴포넌트 등록 | `w·h ≈ 260` contain (블리드 X), 우측 중앙~하단                      | [[1-2-1#2.1\|3D 슬롯 4종]] (`VISUAL_SLOT`)                   |
| **E. 배경**       | 풀 캔버스 외부 사진/스크린샷 1개 (BG) + 보조 3D/이미지(옵션)        | 외부 자료(주제 관련), CdBd 페이지/에디터 캡처                                                  | BG: 전체 `600×450` **opacity 20%** / 보조 `230×230` 우측         | [[1-2-1#2.1\|배경 슬롯 2종]] (`BG_SLOT`·`VISUAL_SLOT`)         |

> **공통 규칙**: 비율 유지 / 텍스트 가리지 않기
> **🆕 기능 이미지 cornerRadius = 16** (2026-06-04): 에디터·관리·통계·답변 등 **CdBd 기능 캡쳐 이미지를 슬롯에 직접 fill** 할 때 슬롯에 `cornerRadius = 16` 적용. 적용 대상: **B 유형 EDITOR_SLOT**(에디터·통계·답변 캡처), **D 유형 직접 fill**(스크린샷 자산일 때만), **E 유형 보조 VISUAL_SLOT**(기능 캡쳐일 때). 적용 제외: 모바일 목업 컴포넌트는 자체 phone 모양에 radius 내장돼 별도 ❌. 3D 아이콘(투명 PNG)도 radius 무관 ❌. BG_SLOT(opacity 20% 풀 배경)도 ❌.
> **🆕 기능 이미지 fit-by-height + 우측 정렬** (2026-06-04): 기능 이미지 fill 시 **이미지 비율 유지 + 슬롯 세로(height) 가득 채우기**. `scaledW = imgW × slotH / imgH` 계산 후 분기:
> - **scaledW > slotW (wider, overflow)** — 슬롯 크기 유지, `scaleMode = "CROP"` + `imageTransform = [[slotW/scaledW, 0, 1 - slotW/scaledW], [0, 1, 0]]`. 좌측 overflow 잘림, 우측 데이터 보존.
> - **scaledW ≤ slotW (narrower)** — 슬롯 자체를 `scaledW × slotH`로 **resize**, 우측 정렬 위치로 이동, `scaleMode = "FILL"` (slot 비율과 image 비율이 같으므로 왜곡 ❌). 우측 정렬 기준은 **mockup left edge** (mockup과 슬롯이 겹치는 레이아웃, 예: B 상단형) 또는 **원 슬롯 right edge** (mockup과 안 겹치는 레이아웃, 예: B 하단형).
> 적용 대상: 위 cornerRadius 규칙과 동일 (B EDITOR_SLOT 등 기능 이미지 모두). 시각적 의도: 답변/통계 캡쳐는 우측에 핵심 데이터(차트·답변 수)가 위치 → 우측 정렬이 의미 보존에 유리. 예약카드 같은 portrait 위젯도 우측 정렬되어 mockup과 시각적 흐름 형성.
> **A·B·C 공통**: 공식 목업 컴포넌트(§1.1.a) **반드시 사용**, 의미 영역 크롭(§1.1.b) 적용
> **상단형 vs 하단형 위치 변형**: 슬롯 페이지 `1:1245`에서 좌표 고정 — [[1-2-1. 레이아웃 규칙]] §2~§3 참고
> **레이아웃 결정 흐름**: 타이틀 → 유형 (§1.1 [[1-2-1. 레이아웃 규칙]]) → 위치 변형 → 슬롯 clone → 이 § 1.1 자산 준비 → fill

#### 1.1.a. 공식 목업 컴포넌트 (3종류) — A·B·C에서 반드시 사용

> **컴포넌트 페이지**: ✨ [컴포넌트](https://www.figma.com/design/3LPfZ6a6vlJrZ4TXorVWJL/CdBd-블로그-썸네일?node-id=1-1343) (`1:1343`) 안의 **`모바일 목업` section** (`1:1344`) — 3개 symbol. **반드시 이 컴포넌트를 인스턴스로 생성/clone해서 사용** (직접 Rectangle 그리기 ❌).

| # | 목업 종류 | 컴포넌트 ID (symbol) | 원본 크기 | 화면 영역 (캡처 fill 위치) | 사용처 |
|---|----------|---------------------|----------|---------------------------|--------|
| 1 | **원페이지 목업-1** | `75:34` ⭐ (2026-05-31 새 마스터, 이전 `1:1368`) | 197.36×415.49 | 컴포넌트 안 **흰색/거의 흰색 rect** (#FFFFFF·#FAFAFA) | 일반 단일 페이지 (명함·프로필 등) |
| 2 | **원페이지 목업-2 (iPhone-16-Pro)** | `1:1369` | 187×444 | 컴포넌트 안 **흰색/거의 흰색 rect** (#FFFFFF·#FAFAFA) | 진짜 폰 느낌 강조 — 프리미엄 컨셉 |
| 3 | **멀티페이지 목업** | `591:646` | 197.69×400 | 컴포넌트 안 **흰색/거의 흰색 rect** (#FFFFFF·#FAFAFA) (중앙 띠) | 다중 페이지 시리즈·스크롤형 콘텐츠 |

> 💡 **공통 규칙**: 3종 목업 모두 **거의 흰색 rect 자식 노드**(#FFFFFF·#FAFAFA 등)가 화면 영역. viewer 캡처는 이 영역에 fill한다.
> **자동화**: 컴포넌트 인스턴스 생성 → 자식 중 `RECTANGLE/FRAME`이면서 `fills`가 회색톤(R≈G≈B)이고 밝기 ≥ 0.9인 노드를 찾아 imageHash로 교체. (Vector 베젤은 VECTOR 타입이라 자동 제외.) 내부 자식 ID는 인스턴스마다 달라질 수 있으므로 **타입+색상 기준 탐색**이 안전.

| 컴포넌트 | 화면 영역 색상 | **screen rect 크기·비율** ⭐ | image fill 위치 |
|----------|----------------|----------------------------|----------------|
| `75:34` 원페이지 목업-1 (구 `1:1368`) | **#FAFAFA** | **181.32 × 399.79** (비율 **0.4535**, 세로 김) | 단일 회색 rect |
| `1:1369` iPhone-16-Pro | 확인 필요 | 확인 필요 | — |
| `591:646` 멀티페이지 | (2026-06-12 측정) | **177 × 271** (비율 **0.6531**, 정사각형에 가까움, 중앙 띠) ⭐ | 2개 fillable rect 있음 — phone(143×325, 비율 0.4392) ❌ + **진짜 screen(중앙 띠, 비율 0.6531)** ⭐ |

**🔴 mockup screen rect 식별 룰 (2026-06-12 추가)**
- `findOne`으로 **첫 발견 회색 rect**에 fill ❌ (`591:646`처럼 phone 본체에 잘못 fill 가능)
- **비율 매칭 기반 식별**:
  - 원페이지(75:34) → 단일 screen rect 0.4535 → 그대로 fill
  - 멀티페이지(591:646) → 2개 fillable rect 중 **0.6531에 가장 가까운 rect** (중앙 띠)만 fill / 다른 rect(0.4392 phone)는 master 원본으로 복원

```js
// 정확한 mockup screen 식별 의사코드
const rects = mockup.findAll(n => n.type === "RECTANGLE" || n.type === "FRAME");
const TARGET_RATIO = mockupName === "591:646" ? 0.6531 : 0.4535;  // mockup 종류별
let best = null, bestDiff = Infinity;
for (const r of rects) {
  const diff = Math.abs(r.width / r.height - TARGET_RATIO);
  if (diff < bestDiff) { bestDiff = diff; best = r; }
}
best.fills = [{ type: "IMAGE", imageHash, scaleMode: "FILL" }];
```

> **선택 가이드**:
> - **명함·프로필·일반 페이지** → 원페이지 목업-1
> - **고급·프리미엄·인물 강조** → 원페이지 목업-2 (iPhone-16-Pro)
> - **카탈로그·룩북·매거진** (페이지 시리즈) → 멀티페이지 목업
>
> **소재집 § 2.2 카탈로그 매핑 (2026-05-26 확정)**:
> - `oak_table` (1) → 원페이지 목업
> - `lookbook` (2) / `newarrival` (3) / `online_lookbook` (4) → **멀티페이지 목업**

**사용 방법** (Figma Plugin API):
1. 공식 컴포넌트(`75:34`/`1:1369`/`591:646`)를 `createInstance()` 또는 `clone()`해서 새 썸네일 프레임에 `appendChild`
2. 인스턴스 내부에서 **`fills`가 `#FFFFFF`(R=1, G=1, B=1)인 rect**를 `findOne`으로 찾아 imageHash fill 적용 (scaleMode·imageTransform → §1.1.b)
3. 위치를 `x=365.5, y=86` 로 이동 (2026-06-12 통일 — 상단/하단 동일 좌표)
4. **🆕 보라 글로우 effect 적용** (2026-06-12, A 유형 한정): `instance.effects = [{type: "DROP_SHADOW", color: {r: 0.4235, g: 0.2980, b: 1, a: 1}, radius: 20, offset: {x: 0, y: 0}, spread: 0, blendMode: "NORMAL", visible: true}]` — 슬롯 페이지 VISUAL_SLOT의 effects를 그대로 복사하는 패턴 권장 (`instance.effects = visualSlot.effects.filter(e => e.type === "DROP_SHADOW")`)
5. cut-off 패턴 시 부모 프레임에 `clipsContent = true` 보장

#### 1.1.c. CROP imageTransform 비율 보존 공식 ⭐ (2026-06-12 추가, 사고 사례 기반)

> **사고**: A 안 2 / C 안 1·2에서 풀페이지 캡처(750×8090, aspect 10.79)를 mockup screen(aspect 2.21)에 `imageTransform: [[1,0,0],[0,1,0.55]]`로 fill → 비율 깨져 모자가 가로 늘어남.
> **원인**: `imageTransform`의 매트릭스는 **슬롯 좌표 → 이미지 좌표 매핑**. y-scale을 1로 두면 이미지 세로 비율 무시 → stretch.

**올바른 매트릭스**:

```
y_scale = slot_aspect / image_aspect
ty 범위 = 0 ~ (1 - y_scale)
imageTransform = [[1, 0, 0], [0, y_scale, ty]]
```

여기서:
- `slot_aspect` = `screen.height / screen.width` (mockup screen rect 비율)
- `image_aspect` = `image.height / image.width` (캡처 원본 비율, `getImageByHash().getSizeAsync()`로 측정)
- `ty` = 콘텐츠 영역의 Y 비율 (0 = 이미지 상단, max = 이미지 하단 직전)

**Plugin API 사용 예시**:

```js
// 1) 이미지 size 측정
const imgSize = await figma.getImageByHash(imageHash).getSizeAsync();
const imgAspect = imgSize.height / imgSize.width;

// 2) screen aspect 측정 (mockup INSTANCE 안의 흰색 rect)
const slotAspect = screen.height / screen.width;

// 3) y-scale 계산 + ty 결정 (콘텐츠 의미 영역)
const yScale = slotAspect / imgAspect;
const ty = 0.55;  // 룩북 카탈로그 (이미지 55% 지점)

// 4) CROP + 비율 보존 매트릭스
screen.fills = [{
  type: "IMAGE",
  imageHash,
  scaleMode: "CROP",
  imageTransform: [[1, 0, 0], [0, yScale, ty]],
}];
```

**ty 결정 — 의미 영역 매핑 (§1.1.b 의미 영역 크롭)**

| 이미지 콘텐츠 위치 | ty 값 (image_aspect=10.79 가정) |
|------------------|-----------------|
| 상단 hero | `0.00 ~ 0.05` |
| 본문/안내 | `~0.20` |
| 중간 콘텐츠 (룩북·상품 grid) | `0.45 ~ 0.55` |
| 하단 (인물·CTA) | `0.65 ~ 0.75` |
| 최하단 (RSVP·푸터) | `0.78 ~ max` |

> max = 1 - y_scale (이 값 초과 시 이미지 끝을 넘어가 검정 영역 표시됨)

**주의 — sparkle·crown 데코 컴포넌트는 별개** ⛔:
- 데코 INSTANCE 안에도 IMAGE fill을 가진 RECTANGLE/FRAME이 있음 (sparkle PNG·crown PNG 등)
- 자동화가 mockup screen만 노려야 하는데 **데코 instance 내부 rect까지 잡으면** y_scale 매트릭스가 데코에도 적용되어 시각 오류 (crown이 가로 늘어남 등)
- **방어 3종 (필수 적용)**:
  1. **mockup INSTANCE 이름 필터**: `instance.name === "원페이지 목업-1"` 또는 `instance.name === "MOCKUP_MASTERS"` 등 정확한 이름 매칭. `instance.name === "sparkle2"` 같은 데코는 제외
  2. **mockup fill 처리 후 데코 추가 순서**: 1) mockup INSTANCE 생성 → 2) screen rect 찾기 + IMAGE fill (CROP + 비율 보존 매트릭스) → 3) **그 다음** 데코 INSTANCE 생성·배치. 순서 보장하면 mockup 처리 시 데코는 존재하지 않음
  3. **PNG/SVG 컴포넌트는 그대로** — 데코 INSTANCE는 createInstance() 후 rescale만, IMAGE fill 건드리지 않음

**자동 적용 워크플로우 (use_figma 모드 권장 시퀀스)** ⭐:

```
1. 슬롯 페이지에서 슬롯 frame clone → 출력 페이지 appendChild
2. 텍스트 fill (TITLE/SUBTITLE)
3. mockup INSTANCE 생성:
   a. createInstance(75:34 또는 591:646)
   b. visualSlot.width로 rescale
   c. visualSlot.x/y로 이동
   d. visualSlot.effects 복사 (보라 글로우 등)
   e. screen rect 찾기 (회색톤 RECTANGLE/FRAME, 멀티페이지면 비율 0.6531 분기)
   f. ⭐ 이미지 size 측정 + y_scale 계산
   g. CROP + 비율 보존 매트릭스 fill
4. mockup INSTANCE 처리 완료 후 데코 INSTANCE 생성·배치 (DECO 슬롯 위치)
5. placeholder 제거 (visualSlot.remove() 등)
```

→ 이 순서면 비율 보존 매트릭스는 mockup에만 적용, 데코는 영향 없음.

```js
// 예시 — 회색톤 + 밝기 0.9 이상 (FFFFFF·FAFAFA 모두 포함)
// 🆕 2026-06-12: use_figma MCP 환경은 sync getNodeById 미지원 → async 사용
const mockup = (await figma.getNodeByIdAsync("75:34")).createInstance()
const screen = mockup.findOne(n => {
  if (n.type !== "RECTANGLE" && n.type !== "FRAME") return false
  const f = n.fills?.[0]
  if (f?.type !== "SOLID") return false
  const { r, g, b } = f.color
  const isGray = Math.abs(r-g) < 0.05 && Math.abs(g-b) < 0.05
  return isGray && r >= 0.9
})
screen.fills = [{ type: "IMAGE", imageHash, scaleMode: "FILL" }]
```

#### 1.1.b. 의미 영역 크롭 원칙 — viewer 전체 ❌ / 타이틀이 말하는 그 섹션 ⭕ 🔴

> **핵심**: 목업·배경 이미지의 캡처를 **viewer 페이지 통째로 넣지 않는다.**
> 타이틀이 말하는 **바로 그 기능·섹션**이 화면에 보이도록 **크롭**한다.
> (CdBd viewer는 세로로 매우 길어, 전체를 fill하면 정작 핵심 섹션이 안 보이고 상단 프로필만 노출됨)

| 순서 | 단계 | 내용 |
|------|------|------|
| 1 | **의미 영역 식별** | 타이틀이 가리키는 기능·섹션이 viewer의 어느 위치(상/중/하단)인지 결정 → [[1-3-2. 이미지 출처]] §3.1 "영역 위치" 표 참조 |
| 2 | **CROP fill** | `scaleMode: 'CROP'` + `imageTransform`(Y offset)으로 그 섹션이 목업 화면에 오도록 이동 |
| 3 | **확인** | 목업 화면 안에 "타이틀이 말하는 그 UI"가 실제로 보이는가 점검 |

> 예: 타이틀이 "**상담 신청 폼**"이면 → `promotion` viewer의 **하단 폼 영역**을 CROP. 프로필·카드가 보이는 상단을 넣으면 ❌
> 영역 좌표·`scaleMode`·`imageTransform` 코드 → [[1-3-2. 이미지 출처]] §3.1 / Microlink CROP 표준 패턴 → §7.2

##### 🆕 A 유형 다중 안 — ty 배분 원칙 (2026-06-12 MASTERS 시안 정립)

> A 유형은 같은 viewer의 다른 의미 영역을 **2안에 명확히 분리**해 사용자가 비교 선택할 수 있게 한다. 두 안 모두 같은 영역(예: 둘 다 중간 콘텐츠)이면 다양성 ❌.

**ty 배분 원칙**:

| 안 | 권장 ty | 영역 |
|------|---------|------|
| **A 안 1 (상단·서브 `1:1269`)** | **ty = 0** | **hero — 브랜드/포스터/행사 정체성** (첫인상·강한 임팩트) |
| **A 안 2 (하단·서브 `1:1246`)** | **ty = 0.55 ~ 0.78** | **CTA/RSVP/폼/구매 영역** (사용자 액션 강조) |

> ⚠️ **회피**: A 안 1·2 모두 중간 콘텐츠(ty=0.3~0.5)에 몰리면 시각 차이가 약해 비교 의미 없음. **hero ↔ CTA 양극단**에 배치해 명확한 비교 제공.

**예시 — 마스터스교회 시안 (2026-06-12)**:
- A 안 1 ty=0 → 교회 로고 + 행사 hero (브랜드 첫인상)
- A 안 2 ty=0.6 → VIP 정보 입력 폼 (참석·RSVP)

**예시 — 올인컴플리트 시안 (2026-06-12)**:
- A 안 1 ty=0 → 봉투 디자인 hero (그래픽 임팩트)
- A 안 2 ty=0.55 → 룩북 카탈로그 (모자 grid)

##### 🆕 C 멀티 목업 — 단일 페이지 viewer를 의미 섹션 3개로 분할 (2026-06-12 INHA 시안 신규)

> C 유형(`1:1287` 멀티·상단·서브)은 본래 멀티페이지 viewer(swiper)용이지만, **단일 페이지 viewer를 의미 섹션으로 잘라 3개 mockup에 각각 다른 영역으로 fill**하는 패턴도 가능. 타이틀이 "한 페이지에 여러 섹션이 다 들어있다"·"행사 흐름" 류일 때 강력.

**적용 조건**:
- **단일 페이지 viewer** (스크롤형) — 멀티페이지 swiper면 [[1-3-2. 이미지 출처]] §2 멀티페이지 mockup 그대로
- 타이틀이 **여러 기능·여러 시간대·여러 단계** 등 **복수 의미**를 함축
- 사람 관점에서 viewer를 봤을 때 자연스러운 **사용자 여정 3단계**가 보임 (정체성 → 핵심 정보 → 액션)

**절차**:
1. **사람 관점 viewer 분석** — 브라우저로 viewer 열어 의미 섹션을 사람이 직접 파악 ([[2. 자동화 체크리스트|2]] §자주 놓치는 #10). 자동 캡처만으로 결정 ❌
2. **3개 의미 섹션 선정** — 자연 흐름:
   - **섹션 1**: 정체성 (hero — 브랜드/행사 정체성·포스터·로고)
   - **섹션 2**: 핵심 정보 (타이틀이 직접 가리키는 그 정보 — 예: 시간·일정·기능 디테일) ⭐
   - **섹션 3**: 액션 (RSVP·구매·문의·CTA 등 사용자 참여 영역)
3. **mockup aspect (≈0.4535)에 맞춰 PIL crop** — 각 섹션을 `width=원본_w / height=원본_w / 0.4535 ≈ 원본_w × 2.205`로 잘라야 mockup screen 안에 비율 보존되어 들어감
4. **3개 PNG 업로드** — `upload_assets(count=3)` 한 번에 3개 hash 확보
5. **각 VISUAL_SLOT_1/2/3에 mockup instance + 해당 hash fill**:
   - `mockupMaster.createInstance()` → `rescale(slot.width / mockupMaster.width)` (예: 143/197.36 ≈ 0.7246)
   - screen rect 찾아 `scaleMode: "CROP"` + 비율 보존 매트릭스 fill
   - 위치를 slot.x, slot.y로 이동 + 슬롯 placeholder hidden + strokes/fills 제거

**PIL crop 의사코드**:
```python
from PIL import Image
im = Image.open('viewer-fullpage.png')
W, H = im.size  # ex: 640 × 5910
SEC_H = int(W / 0.4535)  # ex: 1411

# Sections by y range — 사람 분석 결과
sections = [
    ('sec1-hero',   0,    SEC_H),       # 정체성
    ('sec2-core',   2533, 2533 + SEC_H), # 핵심 정보 (타이틀이 가리키는 그 섹션)
    ('sec3-action', 3944, 3944 + SEC_H), # RSVP/CTA
]
for name, y0, y1 in sections:
    im.crop((0, y0, W, y1)).save(f'.temp/{name}.png')
```

**사례 — INHA 인하대 졸업패션쇼** (viewer 640×5910, 2026-06-12):
- **Mockup 1 (좌, sec1)**: y=0~1411 → 포스터 + "40th Inha Univ. GRADUATION FASHION SHOW" — 행사 정체성
- **Mockup 2 (중, sec2)** ⭐: y=2533~3944 → "행사 스케줄 안내 / 16:00 첫번째 / 19:00 두번째 패션쇼" — 타이틀 **"1일 2회 공연" 직접 매칭**
- **Mockup 3 (우, sec3)**: y=3944~5355 → "정보 입력 / 참석 시간 16:00 / 19:00 / 불참" — RSVP

> 💡 자연 여정: **정체성 → 핵심 정보 → 액션**. 사용자가 모바일 초대장을 받았을 때 시선 흐름과 동일.

---

### 1.2. 보조 이미지 (Sub)
주 이미지를 **보완·강조**하는 요소. 정서·맥락 추가용. 슬롯 16종에는 보조 이미지가 **포함되지 않음** → 필요 시 **라이브러리에서 인스턴스 생성 + 슬롯 위에 동적 배치** (위치 알고리즘 §1.2.a).

| 유형            | 용도         | 예시                      | 크기                       | 라이브러리 위치 |
| ------------- | ---------- | ----------------------- | ------------------------ | ------------ |
| **데코**        | 시선 유도, 임팩트 | **sparkle2/3/4** ✅ (sparkle1 `4:141` ❌ 금지) · magic wind ✅ · crown/shield/calendar/globe/envelope ✅ (chart-up·card·book·chat-bubble·pen 추가 예정) — 화이트리스트는 [[1-2-1. 레이아웃 규칙#2.4]] | **75×75px 고정** (2026-06-12 변경, 이전 50×50) | ✨ [컴포넌트](https://www.figma.com/design/3LPfZ6a6vlJrZ4TXorVWJL/CdBd-블로그-썸네일?node-id=1-1343) → `보조 이미지_데코` section (`4:117`) / [[1-3-2. 이미지 출처]] §4.1 |
| **이모지/캐릭터**   | 정서·감정 강화   | Memoji 사람 1명·그룹·손 | 100~150px | (예정) `보조 이미지_이모지` section TBD / [[1-3-2. 이미지 출처]] §4.2 |
| **앱/서비스 아이콘** | 비교 대상 명시   | 링크트리, Jasper, Lexica 로고 | 60×60px 이하 | 각 서비스 공식 로고 (구글링) → 필요 시 라이브러리에 추가 |
| **UI 요소**     | 맥락 추가      | 채팅 말풍선, 카드, 폼           | 주 이미지의 1/3 이하 | 직접 디자인 또는 [Figma Community](https://www.figma.com/community) |

> **개수**: 5유형별 권장 → §2.1
> **자산 선택**: 타이틀의 **1d 톤·키워드**에 따라 추천 → [[1-3-2. 이미지 출처]] §1d + §5.1

#### 1.2.a. 보조 이미지 위치 알고리즘 (주 이미지 기준 상대 좌표)

> 보조 이미지는 슬롯에 미리 박혀있지 않음 → ✨ 라이브러리에서 인스턴스 생성 후 **주 이미지(슬롯의 `VISUAL_SLOT`) 좌표를 기준으로 상대 배치**.

| 보조 유형 | 크기 | 배치 규칙 (주 이미지 기준) | 좌표 예시 |
|---|---|---|---|
| **데코** (sparkle 등) | **75×75 고정** | **A 유형 슬롯에 박힌 DECO placeholder 활용** (`x503.5/y58.5/75×75`, [[1-2-1. 레이아웃 규칙#2.4]]). placeholder 없는 슬롯(A 하단 등)은 동일 좌표로 신규 생성. 시각 중심점 `(541, 96)`. | A 공통: `x = 503.5 / y = 58.5` |
| **이모지/Memoji** | 100~150 | **주 이미지의 대각선 반대편 빈 자리** (균형). 단독은 좌하단, 그룹은 좌측 중앙. | 좌하단: `x = 60 / y = 280` (캔버스 좌표) |
| **앱 아이콘** | 60×60 이하 | **주 이미지 주변 산포** (좌측 빈 공간에 1~3개 세로·가로 배치, 간격 ≈ 20px) | 좌측 가로 산포: `x = 50/120/190 / y = 360` |
| **UI 요소** (말풍선·카드) | 주의 1/3 이하 | **주 이미지 근처** (옆 또는 위, 살짝 겹침 가능). 의미 연결. | 우측 옆: `x = 주.x - 80 / y = 주.y + 100` |

**일반 원칙 (모든 유형 공통):**
1. **시각 무게**: 보조 < 주 항상 유지
2. **대각선 균형**: 타이틀 ↔ 주 이미지 ↔ 보조 이미지가 한쪽으로 쏠리지 않게
3. **텍스트 회피**: `x ≥ 280` 영역에만 배치 (좌측 텍스트 영역 침범 ❌). 단, **Memoji 그룹**은 예외적으로 좌측 가능 (단, 텍스트와 ≥ 30px 간격)
4. **겹침 허용**: 데코는 주 이미지와 **의도적으로 겹침** (포인트 강조). 다른 유형은 살짝만.

> **자동화 의사코드**:
> ```js
> // 1. 주 이미지 좌표
> const main = slot.findOne(n => n.name === "VISUAL_SLOT")
>
> // 2. 보조 컴포넌트 인스턴스 생성 (라이브러리 페이지 1:1343)
> // 데코 화이트리스트: sparkle2(4:166) / sparkle3(4:165) / sparkle4(4:164) / magic wind(4:167)
> // ⛔ sparkle1(4:141) 사용 금지 — [[1-2-1. 레이아웃 규칙#2.4]]
> const sparkleId = ["4:166", "4:165", "4:164"][Math.floor(Math.random()*3)]  // 다양성
> // 🆕 2026-06-12: use_figma MCP 환경은 sync getNodeById 미지원 → async 사용
> const deco = (await figma.getNodeByIdAsync(sparkleId)).createInstance()
>
> // 3. 75×75 리사이즈 — **rescale 사용** (resize ❌)
> //    이유: instance.resize(75,75)는 외곽 크기만 변경, 자식 vector가 100×100 그대로 → 잘림
> //          instance.rescale(0.75)는 자식까지 비례 75×75 (정확)
> deco.rescale(75 / deco.width)  // 100→75 = factor 0.75
>
> // 4. 위치 — A 유형 슬롯 박힌 DECO placeholder 좌표 (x503.5/y58.5) 또는 동일 좌표 신규
> deco.x = 503.5
> deco.y = 58.5
>
> // 5. 슬롯 프레임에 추가 — ⚠️ 데코는 항상 z-top (children 마지막)
> frame.appendChild(deco)  // mockup instance 추가 이후에 호출되어야 z-top 보장
> ```

#### 🆕 1.2.b. mockup·DECO z-index 순서 (2026-06-19 신설, 사고 사례)

> **Figma children 배열 마지막 = z-top**. 자동화에서 mockup instance를 마지막에 추가하면 DECO가 그 밑으로 가려진다 ⛔.

**표준 frame children 순서** (위 → 아래 = z-top → z-bottom):

| 순서 | 노드 | 비고 |
|---|---|---|
| 1 | **DECO instance** (crown·envelope·sparkle 등) | 최상단 — mockup 위로 살짝 걸침 |
| 2 | mockup instance (75:34 등) | A·B·C 유형 |
| 3 | VISUAL_SLOT / VISUAL_SLOT_1·2·3 | placeholder, fills 비움 |
| 4 | EDITOR_SLOT (B 유형) | 에디터 UI fill |
| 5 | TEXT_BLOCK / TITLE / SUBTITLE | 좌측 텍스트 |

**자동화 안전 패턴**:
```js
// ① 텍스트 fill (TEXT_BLOCK)
setText(frame);

// ② mockup instance 추가 → frame children 마지막
const inst = mockMaster.createInstance();
frame.appendChild(inst);

// ③ DECO instance 추가 또는 기존 DECO를 z-top으로 재정렬
const decoInst = frame.findOne(n =>
  n.type === "INSTANCE" &&
  ["crown", "envelope", "sparkle2", "sparkle3", "sparkle4", "magic wind", "calendar", "globe", "shield", "stethoscope", "document", "paper", "file", "folder", "book"].includes(n.name)
);
if (decoInst) frame.appendChild(decoInst);  // 다시 appendChild → children 마지막 → z-top
```

**사고 사례**: 2026-06-19 VIP 1:1 개인화 시안 A1·A2 — mockup instance 추가 후 crown·envelope 데코가 그 밑으로 내려가 사용자 시각 검수에서 발견. `frame.appendChild(deco)` 재정렬로 해결.

📌 [[2. 자동화 체크리스트|2]] §자주 놓치는 #12와 1:1 동기화.

---

## 2. 이미지 조합 규칙 — 몇 개, 어떻게

### 2.1. 개수 규칙

#### 주 이미지: 반드시 1개
**시각적 초점 분산 금지.** "한 덩어리"로 취급되는 경우도 1개로 셈:
- **C. 멀티 목업** 2~3개 겹침 = 한 덩어리 = **주 이미지 1개**
- **B. 목업+에디터** 결합형 (목업 + UI) = 한 덩어리 = **주 이미지 1개** (→ [[1-2-1. 레이아웃 규칙]] §1.1 결합형 / 캡처 [[1-3-2. 이미지 출처]] §3.2)

#### 보조 이미지: 5유형(A~E)별 권장

| 주 이미지 유형      | 보조 권장 개수                   |
| ------------- | -------------------------- |
| **A. 목업**     | **0~1개** (데코 1)            |
| **B. 목업+에디터** | **1개**                     |
| **C. 멀티 목업**  | **0~1개** (Memoji 그룹 또는 데코) |
| **D. 3D 아이콘** | **0~1개** (데코)              |
| **E. 배경**     | **1개** (보조 3D)             |

> **공통 원칙**: 보조 < 주 (시각 무게). "한 덩어리" 카운팅 — Memoji 그룹·sparkle 세트는 1개로 셈.

---

### 2.2. 조합 원칙

1. **역할이 명확해야 함** — 주/보조 중 어느 것이 핵심인지 한눈에 보여야 함
2. **텍스트와 겹치지 않기** — 이미지가 글자를 가리면 안 됨
3. **여백 확보** — 이미지끼리, 그리고 텍스트와의 여백을 충분히

---

> 주 이미지 배치(좌표·크기·블리드)는 **슬롯이 자동 정의** → [[1-2-1. 레이아웃 규칙]] §2~§3
> 보조 이미지 위치 알고리즘 → §1.2.a

---

## 갱신 로그

| 일자 | 변경 내용 |
|------|-----------|
| 2026-05-29 | 초기 — 주/보조 이미지 분류, 5유형 자산 표, 의미 영역 크롭 원칙 (§1.1.b) |
| 2026-05-31 | 새 Figma 파일 + 보조 이미지 라이브러리 구조 (`1:1343` ✨ 컴포넌트). 새 마스터 `75:34` 도입. |
| 2026-06-04 | 기능 이미지 `cornerRadius=16` + fit-by-height + 우측 정렬 규칙 (B EDITOR_SLOT 등). |
| 2026-06-12 | **잔존 옛 정보 정리** — ① §1.1.a 화면 영역 색상 표·사용 방법에서 옛 `1:1368` → `75:34` 갱신 ② §1.2 데코 표를 등록 현황(crown·shield·calendar·globe·envelope·magic wind)과 일치하게 갱신 ③ §1.2.a 의사코드: `figma.getNodeById` → `await figma.getNodeByIdAsync` (use_figma MCP async 필수), sparkle1(`4:141`) 제거 → sparkle2/3/4 화이트리스트만 — [[1-2-1. 레이아웃 규칙#2.4]]. |
| 2026-06-19 | **§1.2.b mockup·DECO z-index 표준 순서 신설** — VIP 1:1 개인화 시안 A1·A2 사고로 정착. Figma children 마지막 = z-top 룰 + 자동화 안전 패턴 의사코드 (mockup 추가 후 `frame.appendChild(deco)` 재정렬). §1.2.a 의사코드도 동기화 (deco는 `frame.appendChild`로 마지막에 추가). [[2. 자동화 체크리스트]] §자주 놓치는 #12와 1:1 매칭. |
| 2026-06-12 | **§1.1.b A 유형 다중 안 ty 배분 원칙 신설** — A 안 1·2의 시각 다양성 보장. 안 1 ty=0 (hero·브랜드 정체성) / 안 2 ty=0.55~0.78 (CTA·RSVP·폼). 중간 콘텐츠 양쪽 몰림 ❌. 사례: MASTERS (교회 hero / VIP 폼) + 올인컴플리트 (봉투 hero / 룩북). |
