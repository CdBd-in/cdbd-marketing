# CLAUDE.md — CdBd 블로그 작업 표준 규칙 (항상 준수)

> 이 파일은 매 세션 자동 로드된다. 아래는 사용자가 **반복 강조한** 필수 규칙이므로 예외 없이 따른다.

## 🔴 블로그 썸네일을 만들 때 — 반드시

### 1. 시안 개수는 항상 4~6안 (단일 안 ❌)
"블로그 썸네일 만들어줘" 요청을 받으면 **유형 2~3개 × 각 유형 2~3안 = 총 4~6안**을 만들어 보여준다.
- 한 개만 만들지 말 것. 유형 다양화가 핵심(예: B 목업+에디터 / A 단일목업 / C 멀티 / D 3D 중 2~3개).
- 근거: `블로그/썸네일/2. 자동화 체크리스트.md` §1 사전 3원칙 ①.

### 2. 목업 종류 매칭 — 멀티페이지 viewer는 반드시 `591:646` (반복 실수 지점 ⚠️)
- **단일 페이지 viewer** (`oak_table`) → 원페이지 목업 **`75:34`**
- **멀티페이지 viewer** (`newarrival`·`lookbook`·`online_lookbook` — 가로 스와이프) → **멀티페이지 목업 컴포넌트 `591:646`** (구 `1:1370` 폐기, 2026-06-22 교체)
  - **캡처 규칙**: 페이지 **최상단(로고/헤더)부터 하단 네비게이터(`N/4` 페이저 + 진행바)까지 통째로** 캡처. 외곽 흰/회색 여백만 트림하고 **네비게이터는 절대 잘라내지 말 것**.
  - **채움 규칙 — #ffffff 영역 남김없이 (edge-to-edge)**: `591:646`의 흰 띠 `Rectangle 3424`(`591:644`)는 화면 **중앙 띠만** 덮어 상/하단이 빈다. 따라서 **전체 화면 노드 `Change-This`(`591:643`)에 풀페이지 캡처를 FILL** + **`Rectangle 3424`는 `fills=[]`(투명)** 처리해 헤더(최상단)~네비게이터(최하단)가 화면을 빈틈없이 채우게 한다. (인스턴스에서 rect 크기/위치는 override 불가 → `Change-This` fill 방식 사용)
- **C 멀티목업**(폰 3대) → `75:34` ×3 (각 폰이 1페이지)
- ❌ **멀티페이지 viewer를 `75:34`(원페이지)에 넣지 말 것** — 사용자가 여러 번 지적한 실수.
- 근거: `블로그/썸네일/1. 디자인 가이드/1-3. 이미지/1-3-2. 이미지 출처.md` / 자동화 체크리스트 §3-D-2.

### 3. 출력 위치
새로 생성하는 썸네일은 출력 페이지 `26:2`의 **y ≥ 23537** 에 배치 (기존 작업물과 겹치지 않게).

### 4. 텍스트 규칙
- 타이틀 Pretendard **Bold 36px** (임의 축소 ❌), 서브 Medium 24px.
- 원문 그대로 — `\n`(줄바꿈)만 추가, 글자·구두점 변경 ❌.
- 강조 **1영역**만 퍼플 `#8F80FF`(의미 단위, 한 줄에 통째로) / 서브 전체 그린 `#4DE98B`.
- em dash(—)·시리즈 마커는 SUBTITLE/TITLE 분리 후보 → 분리 시 사용자 컨펌.
- 긴 타이틀 때문에 슬롯 기본 배치(아이콘·목업 위치)를 임의로 바꾸지 말 것 — 슬롯 기본 좌표 유지.

## 📁 상세 가이드 위치
- 썸네일: `블로그/썸네일/1. 디자인 가이드/` + `블로그/썸네일/2. 자동화 체크리스트.md`
- 본문 이미지: `블로그/이미지/1. 디자인 가이드/`
- 플러그인: `자동화 도구/cdbd-thumbnail-plugin/`
