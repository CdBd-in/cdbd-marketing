# CLAUDE.md — CdBd 블로그 작업 표준 규칙 (항상 준수)

> 이 파일은 매 세션 자동 로드된다. 아래는 사용자가 **반복 강조한** 필수 규칙이므로 예외 없이 따른다.

## 🔴 이미지(썸네일·본문)를 만들 때 — 반드시 먼저

**제작 시작 전, 해당 피드백 기준 파일을 반드시 먼저 읽는다** — 썸네일 `1. 블로그/1-1. 썸네일/1-1-6. 피드백 기준.md` · 본문 `1. 블로그/1-2. 본문/1-2-4. 피드백 기준.md`. 여기 누적된 피드백을 참고해 같은 지적을 반복하지 않는다.

## 🔴 블로그 썸네일을 만들 때 — 반드시

### 1. 시안 개수는 항상 4~6안 (단일 안 ❌)
"블로그 썸네일 만들어줘" 요청을 받으면 **유형 2~3개 × 각 유형 2~3안 = 총 4~6안**을 만들어 보여준다.
- 한 개만 만들지 말 것. 유형 다양화가 핵심(예: B 목업+에디터 / A 단일목업 / C 멀티 / D 3D 중 2~3개).
- 근거: `1. 블로그/1-1. 썸네일/1-1-4. 검수 체크리스트.md` §1 사전 3원칙 ①.

### 2. 목업 종류 매칭 — 멀티페이지 viewer는 반드시 `338:3238` (반복 실수 지점 ⚠️)
- **단일 페이지 viewer** (`oak_table`) → 원페이지 목업 **`338:3224`**
- **멀티페이지 viewer** (`newarrival`·`lookbook`·`online_lookbook` — 가로 스와이프) → **멀티페이지 목업 컴포넌트 `338:3238`** (구 `1:1370` 폐기, 2026-06-22 교체)
  - **캡처 규칙**: 페이지 **최상단(로고/헤더)부터 하단 네비게이터(`N/4` 페이저 + 진행바)까지 통째로** 캡처. 외곽 흰/회색 여백만 트림하고 **네비게이터는 절대 잘라내지 말 것**.
  - **채움 규칙 — #ffffff 영역 남김없이 (edge-to-edge)**: 멀티페이지 목업 `338:3238`의 #ffffff 화면 영역은 내부 **`Change-This` 노드**(`338:3245` 중앙 화면 · `338:3246` 하단)에 **풀페이지 캡처(헤더 최상단~네비게이터 최하단)를 FILL**해 빈틈없이 채운다. ⚠️ **2026-06-25 컴포넌트 재설계** — 구 `Rectangle 3424` 투명 처리 방식은 폐기, 새 컴포넌트는 `Change-This`에 직접 fill. 첫 사용 시 채움 결과 1회 확인 권장.
- **C 멀티목업**(폰 3대) → `338:3224` ×3 (각 폰이 1페이지)
- ❌ **멀티페이지 viewer를 `338:3224`(원페이지)에 넣지 말 것** — 사용자가 여러 번 지적한 실수.
- 🔴🔴 **캡처본은 반드시 "목업 프레임 안"에 넣는다 (2026-07-22 재지적)** — 납작한 이미지 fill로 슬롯에 때우면 **폰 목업이 빠져 보인다**("목업도 다 빠졌는데"). 캡처 삽입 시 ①뷰어 종류에 맞는 목업 선택(단일→`338:3224` / 멀티→`338:3238`) ②목업 스크린에 fill ③멀티는 헤더~네비게이터 통째 캡처 규칙 준수 — 이 순서를 **무조건** 지킨다.
- **단일 목업(A형) 세트도 만든다** — 다중(C)만이 아니라 목업 1개짜리 A형 버전도 포함.
- 근거: `1. 블로그/1-1. 썸네일/1-1-3. 이미지 규칙.md` / 검수 체크리스트 §3-D-2.

### 3. 텍스트 규칙  ⚠️ 2026-09-02 개정 — §「최신 기준」이 우선
- 타이틀 Pretendard **Bold 36px** (임의 축소 ❌), 서브 Medium 24px.
- 강조 **1영역**만 퍼플 `#8F80FF` / 서브 전체 그린 `#4DE98B`.
- 긴 타이틀 때문에 슬롯 기본 배치(아이콘·목업 위치)를 임의로 바꾸지 말 것 — 슬롯 기본 좌표 유지.
- ~~원문 그대로·`\n`만 추가~~ / ~~강조는 한 줄에 통째로~~ → **아래 §최신 기준으로 대체** (본문 이미지는 원문 골격 유지 규칙 그대로).

## 🔴🔴 최신 기준 — 2026-09-02 채택본 4종 (「실제 이용사례 #1~#4」)

> 사용자가 직접 완성한 `1227:4895` · `1259:3052` · `1221:3013` · `1255:3449` 를 **다음 제작의 품질 기준**으로 지정.
> **아래 항목은 이 파일 위쪽의 일부 규칙을 명시적으로 덮어쓴다.** 상세 → `1. 블로그/1-1. 썸네일/1-1-6. 피드백 기준.md` §7

1. **프레임**: `600×450` · bg `#141414` · **`cornerRadius 24`** (radius 0 ❌)
2. **SUBTITLE = `[실제 이용사례 #N] <주제 축약>` 1줄** (폭 183~416). ~~마커/원문 2줄 분리~~ · ~~폭 330 상한~~ 폐기
3. **TITLE = 썸네일용 재작성 허용** (2~4줄 · 폭 282~460). 원문(`draftPost.title`)에서 **줄이는 방향만**, 새 개념 추가 ❌
   - 예 `반복 워크플로`→`반복 업무` · `한 카드에 담는 법`→`한눈에 담는 법`
4. **강조 = 의미 단위** — 줄 일부·2줄 연속 모두 OK. ~~"한 줄 통째"~~ 폐기
5. 🔴 **블리드 금지** — 모든 오브젝트를 프레임 안에서 마감. 폰 2대는 **바닥 정렬 + 크기 차이**(112.6 ↔ 150.1)로 원근. ~~하단 블리드~~ 폐기
6. **점선 커넥터 = `Vector 129` 클론 · GRADIENT_LINEAR stroke · w2~3 · dash [5,5]** (SOLID 퍼플 dash[7,7]/[12,12] ❌)
7. **3D 아이콘 90~166px** (200px 이상 ❌) · sparkle은 `338:3250 sparkle2` 또는 3d Icons `sparkle` **90×90**
8. **URL 분기 = pill 칩** — 기준 흰 pill `131×26 r100`(Medium 15 `#636363`) + 퍼플 `#6C4CFF` pill `76×26 r100`(흰 Medium 15) + GRADIENT 커넥터 w2. 점·선·텍스트 나열 ❌
9. 🆕 **F형 합산형** — "여러 요소를 한 곳에" 글은 **폰 목업 없이 3D를 `+`(32×32 흰 벡터)로 잇는다** (가로 한 줄 529×160)
10. 자사 결과물 오브젝트엔 **CdBd 로고** (봉투 222×268 + 로고 65.8×15.4)

## 🔴 소재 선택 · 카피 — 반드시 (2026-08-19 추가, 6안 전량 미채택 후)

### 1. 소재 = 글의 **업종·문서 종류**와 같은 실제 템플릿
- 목업 종류(단일/멀티)만 맞추면 끝이 아니다. **글이 말하는 문서가 회사소개서면 회사소개서 템플릿**을 쓴다.
- ❌ 실제 사고: B2B "회사소개서·카탈로그" 글에 `startup-deck`(SaaS IR 피치덱)·`newarrival`(스무디 화보)를 씀 → 사용자가 **여백 YEOBAEK(인테리어 시공사 회사소개서)** 로 전량 교체.
- ⚠️ **`자동화 도구/cdbd-capture/templates-list.json`(19개)과 `1. 블로그/1-3. 템플릿 스크린샷/`은 stale** — 여백 YEOBAEK이 둘 다에 없다.
  → **소재를 고르기 전 `node discover-v2.mjs` 또는 `https://www.cdbd.in/templates` 로 최신 목록을 먼저 확인**하고, 딱 맞는 게 없으면 **사용자에게 묻는다**(비슷한 걸로 임의 대체 ❌).

### 2. 본문 — 화면 자리에 개념 도식을 넣지 말 것
- 글이 "이 자료를 모바일로 만든다"이면 hero·구분 파트는 **실제 뷰어 화면 3단 목업**(①기본 `383:5912` / ④나열 `442:2890`)이 우선.
- ❌ 3D 아이콘 3열 카테고라이징·인쇄물 비교 컴포넌트로 대체 → 교체당함. 순수 도식(그리드·타임라인)은 통과.

### 3. 멀티페이지 목업은 **디태치 + `Group 2013`(캡처+네비게이터) 구조**로
- 1920 캔버스에서 **512×1036** (원본 `338:3238` 의 2.59배), 캡처 rect 455×697(0.653) + 네비 44.
- 3폰 `x=133/711/1288`. 기본형은 중앙만 `y=102`, 나열형은 전부 `y=232` + 퍼플 pill `1페이지/2페이지/3페이지`.
- 썸네일에서는 **139×280 · 2대**(`288,193`·`437,193`) 하단 블리드도 표준 배치.

### 4. 카피 — 템플릿 문장 **골격 유지, 명사만 교체**
- 문장을 통째 새로 쓰지 않는다. ❌ "사원명 엑셀 업로드 → '개별 URL' 자동 생성" / ⭕ **"사원별 '전용 링크' 자동 생성"**(원본 "고객별 '전용 링크' 활성화" 골격).
- URL 슬러그도 원본 유지(`cdbd.in/brand/`), 시즌 값만 교체.
- 🔴 **가상 인물 실명 금지** → 익명 라벨(`A 고객` → **`영업 A`**).
- **병렬 골격은 4개 전부** 같은 축. ❌ 강남지점/판교지점/부산지점/**전시부스** → ⭕ 수도권·충청·영남·호남 **담당**.
- 도식 타이틀은 **수치 + 화살표**: **"표준 카탈로그 1개 → 영업사원별 개인 URL 연결"**.
- 스텝 라벨은 **`명사 + 동사`** 로 통일: ❌ `브랜드 톤` → ⭕ `브랜드 톤 적용`.

### 5. 썸네일 텍스트
- **강조는 한 줄 통째**(줄바꿈 문자 포함). ❌ `인쇄 없이`만 → ⭕ `인쇄 없이 모바일로\n`.
- 겹침이 걱정돼도 **줄을 잘게 쪼개지 말 것**(4줄 ❌ → 3줄, 폭 289px까지 OK). 폭은 두고 **목업을 아래·우측으로 내려 세로 분리**.
- 데코는 75×75 아이콘 1개가 전부가 아니다 — 개념이 안 서면 **아이콘 + 보조 그래픽 조합**(예: 폴더 + 네트워크 발산, 221×161 좌하단).
- 채택본 **프레임 이름 = 타이틀 본문**.

## 🔴 E(배경 실사) 레이아웃 — 2026-08-20 추가 (2연속 목업안 미채택 후)

**썸네일 4~6안을 목업 계열(A·C·D)로만 채우지 말 것. E형을 반드시 후보에 넣는다.**
- 🔴 글 소재가 **계절·명절·선물·감성·실물**이면 **E형이 1순위**. (「명절 기업 선물 카탈로그」 채택본 `1041:6203`)
- **배경 = 글 주제의 실물 장면을 OpenAI로 생성** (CdBd 목업 캡처 ❌).
- **딤 = 배경 rect의 fills 2겹** — `[{IMAGE, FILL, opacity 1}, {SOLID #000000, opacity 0.65}]`. ~~BG_SLOT opacity 20%~~ 폐기.
- 배경 rect는 **프레임보다 크게** 얹어 구도를 크롭 (예 `926×692 @ (-33,-144)`).
- **텍스트 색은 다른 유형과 동일** — 서브 그린 `#4DE98B` + 강조 퍼플 `#8F80FF`. ~~E형 전부 흰색~~ 폐기.
- **역할 분담**: 배경이 소재(선물)를 말하면 **3D 오브젝트는 훅(모바일로 전환)** 을 말한다. 둘 다 소재를 말하면 중복.
- **겹침 해소**: A형은 목업을 아래로 내린다 / **E·D형은 줄을 늘려 폭을 줄인다**(오브젝트가 세로 중앙 고정이라 피할 수 없음).

## 🔴 2026-08-20 추가 — 방문체크 4안 미채택 · 본문 2장 재작업 후

### 1. 썸네일 — 선언·주장형 타이틀이면 **D형(3D 오브젝트)** 을 1순위 안에 반드시 넣는다
- 타이틀에 **"진짜 / 이유 / 왜 / 완결"** 같은 선언·주장 신호어가 있으면 → **D형 1순위**. 본문에 화면이 많아도 무관.
- ❌ 실제 사고: 「초대장의 진짜 완결은 '왔는지' 확인이에요」에 폰 목업 4안(A·2폰·C)만 냄 → 사용자는 **폰 목업 0개**의 D형 채택(`1080:7168`).
- D형 구성 3원칙:
  1. **오브젝트 2개 + 연결선** — 개념을 물건이 아니라 **관계**로 그린다 (QR --점선 곡선 화살표--> 봉투).
  2. **주·부 크기 비대칭** — *결과물* 크게(봉투 217×262) / *수단* 작게(QR 135×135). 동등 크기 ❌.
  3. **자사 제품 오브젝트엔 CdBd 로고**를 얹고, sparkle(`Property=easy`)은 **주 오브젝트에 겹쳐서**.
- SUBTITLE은 **폭 386 한 줄** 유지, TITLE은 **2줄 · 둘째 줄 통째 퍼플**.

### 2. 본문 — **CdBd 서비스 화면은 캡처 금지, Figma로 재구축**
| 대상 | 방법 |
|---|---|
| **템플릿 뷰어**(초대장·카탈로그 = 고객 결과물) | 실제 캡처 그대로 (viewport를 목업 ar에 맞춰 재캡처) |
| **CdBd 서비스**(예약 완료·통계·에디터) | 🔴 **Figma로 UI 재구축 + 글의 세계관 데이터로 재작성** |

- 재구축 데이터: 행사·회사명·주소·호스트·URL 슬러그를 **글 소재와 100% 일치**시킨다.
- **표·리스트는 8~10행**, **상태를 섞는다**(방문 완료 컬러 + 미방문 회색). 1~2행은 휑하고 데모처럼 보임.
- 화면은 **결과 상태**로 (`방문 체크하기`(전) ❌ → `✓ 방문 체크 완료`(후) ⭕).

### 3. 목업 파손 방지 — **종횡비를 먼저 재고 소스를 맞춘다**
1. 목업 **클립 프레임 ar 실측** — ①기본 3단 `481×1036=0.4643` · ④나열 3단 `450×970=0.4643` · 모바일+데스크탑 폰 `436×942=0.4628` · 노트북 `1303×842=1.5475`
2. **소스를 그 ar로** — 재캡처 가능하면 viewport를 ar에 맞춰(`390×840` @DPR3), 불가하면 **가장자리 균일색 패딩**
3. rect를 **클립 크기로 resize + x,y=0** → `FILL` → 잘림 0
4. 노트북처럼 프레임 밖으로 **블리드**하는 목업은 소스 우측에 여백을 패딩해 **블리드가 콘텐츠 대신 여백을 먹게**

### 4. 프로세스(⑤ 사용법 `383:5752`) 스텝 구성
- 폰 전체를 작게 넣지 말고 **액션 영역만 크롭 + 퍼플 점선 마킹 박스 + 커서 아이콘**.
- 마지막 스텝은 **결과 화면**. 설정 폼 카드는 빈 placeholder여도 통과.
- 스텝 라벨 동사는 **기능 상태 변화**로 (`누르기` ❌ → `활성화` ⭕).

### 5. 명명 — 프레임 이름 = **`타이틀 본문` + 2자리 순번**
`CdBd 방문체크 QR로 완성하는 3단 초대장01` · `...02` / Section 이름 = 타이틀 본문.

## 🔴 2026-08-20 추가 (RSVP 글 — 썸네일 6안 전량 미채택 + 본문 2/4 교체 후)

### 1. 텍스트 원문 = `draftPost.title` — SEO 제목 쓰지 말 것 🔴🔴
Wix에서 글을 읽으면 제목이 **두 개** 있다. 썸네일·본문 텍스트의 원문은 **`draftPost.title`(본문 제목)** 뿐이다.
- ❌ `seoData.tags[type=title]` = 검색 노출용 변형 → 글자·어미가 다르다 (`열람 데이터 리마인드 자동화`)
- ⭕ `draftPost.title` = 본문 제목 (`열람 확인 데이터로 재초대·리마인드 자동화`)
- em dash 분리도 **본문 제목 기준**(앞→SUBTITLE / 뒤→TITLE).
- **폭이 안 맞아 줄을 쪼개고 싶으면, 쪼개기 전에 출처가 본문 제목인지 먼저 확인.** SEO 제목이 길어서 생긴 가짜 폭 문제였던 사례가 있다.

### 2. 관리·에디터 화면은 **캡처 재사용 ❌ → UI 킷 조립** 🔴🔴
통계·답변·대시보드 화면이 필요하면 **stale 캡처를 찾지 말고 이 파일의 제품 UI 컴포넌트로 조립**한다.
- 마스터: **`159:6406` COMPONENT 「페이지 통계」** 1920×1336 (내부 `159:6159 Analytics`)
- 부품: `159:5511 Nav/Header/Subpage` · `1080:10680 Nav/Sidebar/Sub` · `159:5380 Button/Basic` · `1080:10674 Card/Statistic/Data URL` · `1080:7599 Input/Text Field/Vertical` · `159:5662 Icon/General/Graph` · 화면 내 `Table` 셀 프레임
- 🔴 **화면 속 데이터는 글의 스토리와 1:1** — 행사명은 폰 목업의 초대장과 동일, URL은 **소재 템플릿의 실제 슬러그**(`invitation/prestige` → `cdbd.in/prestige/001`), 요약 카드 수치 = 표 행수, **파생 수치는 실제 계산**(클릭률=클릭수/페이지뷰).
- 글이 "미열람자를 골라낸다"면 표에 **페이지뷰 0인 행을 실제로 만들어** 데이터로 보여준다.
- ⚠️ 컴포넌트는 계속 추가된다(`1080:*`는 2026-08-20 신규) → **매번 목록을 다시 조회**.

### 3. 목업 자산 = **이미지판**(`1:10623 원페이지 목업` 계열) 🔴
- 폰 바디 `Mockup-Button(X)` IMAGE(FILL) hash `40518949ffe401d00d4bc38fa3c709fe1c606104` + 캡처 rect IMAGE(**CROP**)
- **비율 상수**: 스크린 inset = 폭×**0.0288** · radius = 폭×**0.1298**
- 노트북 hash `9eb8ba6b454efcf6cc248d53d44854904e0b52fc` (1590×970 · 스크린 1303×841 @ +143,+21)
- 🔴 베젤이 실물이라 **어두운 캡처도 실루엣이 살아난다.** 벡터판(`338:3224`)에 스트로크·글로우를 덧대 보정하지 말고 **자산을 바꾼다.**

### 4. 썸네일 배치 — 네 꼭짓점을 다 쓴다
좌상단 텍스트(`TEXT_BLOCK` 40,40 · **폭 330까지 OK**) → 좌하단 보조 그래픽(~229×214) → 우측 **폰 2대 계단 배치**(좌하 하단 블리드 + 우상) → 우상단 3D 아이콘(**~131px**, `377:5070 3d Icons` 인스턴스 우선).
데코는 **2점 세트** — 1점만 넣으면 휑하다.

### 5. ②강조 "버튼 → 결과" 4요소
①전체 딤 `#000` **20%** ②스포트라이트 퍼플 점선 `[12,12]` w8 ③커넥터 퍼플 점선 `[16,16]` w8 ④결과물 퍼플 **실선** w8.
(E형 썸네일 딤 65%와 다름 — 여긴 화면을 계속 읽혀야 하므로 약하게)

### 6. 도식 텍스트 3층 — 그림에 없는 걸 약속하지 말 것
타이틀 = **수치 + 화살표 + 실제 성과** / 스텝 제목 = **무엇을 하나**(명사+동사) / 스텝 설명 = **무엇을 보나**(명사+동사, 추상어·명사구 ❌).
🔴 **스테이지를 지우거나 추가하면 타이틀의 수치·구절도 함께 고친다.**
폭 상한(스테이지 286px): 제목 fs48 ≤6글자 · 설명 fs29 ≤11글자.

## 📁 상세 가이드 위치
- 썸네일: `1. 블로그/1-1. 썸네일/` (스타일·레이아웃·이미지 규칙 + `1-1-4. 검수 체크리스트.md`)
- 본문 이미지: `1. 블로그/1-2. 본문/` (주제·스타일·유형(5목적)·이미지 규칙·카피 규칙)
- 플러그인: `자동화 도구/cdbd-thumbnail-plugin/`
