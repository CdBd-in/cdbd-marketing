---
figma: https://www.figma.com/design/3LPfZ6a6vlJrZ4TXorVWJL/CdBd-블로그-썸네일
updated: 2026-06-04
---

# ✅ 자동화 체크리스트

> 블로그 썸네일 자동 생성 시 **매번 확인해야 할 항목**.
> Claude가 이미지 제작 **전·중·후** 모든 단계에서 체크하며 진행.
> 이 체크리스트는 [[1-1. 스타일]] / [[1-2-1. 레이아웃 규칙]] / [[1-3-1. 이미지 규칙]] / [[1-3-2. 이미지 출처]] 의 핵심 규칙을 한눈에 보기 위함.
>
> 📌 **목표·비전** → [[0. 목표와 비전]] / **디자인 가이드 전체** → `1. 디자인 가이드/` 폴더

---

# 🚨🚨🚨 텍스트 삽입 절대 원칙 (사고 최다 발생 지점)

> **여기서 사고가 가장 많이 난다.** 다른 모든 항목보다 먼저, 매 작업마다 이 3가지부터.

## ① 줄 나누기 → **무조건 의미 단위로**
- 어절·구문 단위로 끊기. 의미가 흐트러지는 위치에서 줄바꿈 ❌
- 강조어는 한 줄 안에 통째로. 두 줄에 걸치지 않기.
- ⛔ **원본 텍스트 변경 금지** — `\n` **추가만** 허용. 쉼표·마침표·느낌표·물음표·글자·띄어쓰기 일체 **추가·삭제·변경 ❌**. 받은 타이틀을 그대로 쪼개기만 한다. (2026-06-04 사고: 쉼표 임의 추가 → 임의 삭제 → 결국 원본 그대로 룰로 단순화)

## ② 어절이 중간에 잘리면 안 됨 ⛔
- "고객도 / 지치는 전화" ✅ / "고객도 지치는 / 전화" ✅
- "고객도 지치는 전 / 화 핑퐁" ❌ ← 자동 줄바꿈에 맡기면 이 사고 남
- 방어: `\n` 명시 + 각 줄 폭 ≤350px 실측 검수 + `textAutoResize=HEIGHT` + `node.resize(350)`

## ③ 최대한 **2-3줄 안에** 정렬
- 기본: **2-3줄**. 
- **예외 — 4줄로 늘리는 유일한 조건**: 2-3줄로 정렬했는데 이미지(VISUAL_SLOT / EDITOR_SLOT / 보조 데코)와 겹칠 것 같을 때.
  - 충돌 검증식: `줄수 × 46 + y_start ≤ 다음 슬롯 y_start`
  - 충돌 시 → 4줄로 분산 (5줄 이상은 ❌)

---

## 🚨 자주 놓치는 핵심 항목 (사용자 강조)

> 위 §텍스트 삽입 절대 원칙을 통과한 후 추가로 더블체크할 항목:

1. **타이틀 → 레이아웃 선정 ([[1-2-1]] §1.1)** — 임의 추측 ❌ / 가이드 신호어 매칭 ⭕
2. **가이드 표 그대로 사용** — 키워드 매핑은 [[1-3-2]] §5.1·§5.4 표만 인용. 코드/즉석 추가 ❌
3. **보조 3D 아이콘은 Thiings에서** — AI 생성 ❌ / [[1-3-2. 이미지 출처]] §4.1 카탈로그 사용 ⭕
4. **서브타이틀 무조건 1줄** — [[1-1. 스타일]] §3 "텍스트 작성 규칙"와 통합 완료 (2026-06-12). 1줄 안 들어가면 ① `SUBTITLE` width를 **460px까지 확장** — 단 **mockup(x=365.5 시작)과 겹치는지 확인** → A 유형 슬롯의 경우 width 460(x40+460=500)이 mockup 좌측 134.5px 침범 → 시각 충돌 ② mockup 겹치면 **서브 2줄 허용** ([[1-1. 스타일]] §3 "최대 2줄") + width 285(표준)로 의미 단위 `\n` 명시 ③ 그래도 의미 손상되면 입력 단계에서 사용자에게 서브 축약 요청. (2026-06-12 PICOHI "학회 KOL 위촉식과 학술 세션을 한 페이지에" 19자 사례에서 정립)
5. **TITLE 강조 키워드는 항상 1개** — [[1-1. 스타일]] §3와 동일 정책 (2026-06-12 확정). 2개 이상 ❌. [[3. 검수 체크리스트]] ④와도 일치.
6. **sparkle은 화이트리스트만** — sparkle2(`4:166`) / sparkle3(`4:165`) / sparkle4(`4:164`) 3종만 사용. **sparkle1 (`4:141`) ❌ 금지** — [[1-2-1. 레이아웃 규칙#2.4]] 화이트리스트.
7. **데코 다양성 — 톤 매칭 우선, sparkle은 fallback** (2026-06-12 추가) — 매번 같은 sparkle 반복 ❌. 선택 순서:
   - **① 타이틀 톤 매칭 우선** ([[1-3-2. 이미지 출처]] §1d 표): "VIP·프리미엄·고급" → **crown** / "안전·보안·신뢰" → **shield** / "AI·자동화·스마트" → **magic-wand** / "글로벌·해외·다국어" → **globe** / "초대·행사·일정·기념" → **calendar** (또는 envelope, 컴포넌트화 시) / "성장·성공·매출" → **chart-up** (등록 시) / "팀·협업·전체" → **Memoji 그룹·hands** (등록 시)
   - **② 톤 매칭 없을 때만 sparkle** (`4:166/4:165/4:164` 라운드 로빈으로 다양성) — fallback
   - **③ 같은 시안 안에서도 변형 권장** — 안 1과 안 2에 다른 데코 (가이드 §5.4 다양성)
   - **사례**: 2026-06-12 INHA·MASTERS·PICOHI 시안이 모두 sparkle2/3 반복만 사용 → 시각 단조 → 룰 정착. 다음 시안부터는 톤 신호어 우선 매칭 + sparkle은 진짜 매칭 없을 때만.

---

## 🚦 사용자 검수 항목 (자동화 후 수동 확인)

자동화가 100% 완벽할 수 없으므로, 사용자가 마지막에 확인:

1. **이미지가 타이틀의 진짜 의미를 담고 있나?**
2. **줄바꿈이 자연스럽나? 강조어가 한 줄에 있나?**
3. **시각적 균형이 맞나? 한쪽으로 쏠리지 않았나?**
4. **2-4안 중 어느 게 가장 적합한가?**

---

## 📋 자동화 지원 현황 (2026-06-04)

자동화는 두 단계로 나뉩니다:
- **슬롯 fill 자동화**: 슬롯 clone → 텍스트/이미지/색상 자동 적용
- **이미지 자동 소싱**: 어디서 이미지를 가져올지 (자동 vs 수동 imageHash 입력)

| 유형 | 슬롯 fill | 이미지 자동 소싱 | 비고 |
|------|----------|-----------------|------|
| **A. 목업** | ✅ `applyMockup()` (목업 인스턴스 + CROP+imageTransform) | ✅ 완전 자동 | **Microlink + cdbd.in viewer 캡처 자동** (가이드 §3.1·§2 17개 매핑) — 무료 50 req/day |
| **B. 목업+에디터** | ✅ `applyEditor()` (EDITOR_SLOT FIT fill) + `applyMockup()` | ❌ 수동 입력 | 슬롯 fill 완성 (2026-06-01). 에디터 캡처 (Playwright) 자동 호출 미구현 |
| **C. 멀티 목업** | ⚠️ VISUAL_SLOT_1/2/3 처리 별도 필요 (플러그인 미구현) / **use_figma 직접 모드 ✅ 2026-06-12 첫 시험 통과** | ❌ 수동 입력 (use_figma + upload_assets 6장 POST 패턴) | viewer 2-3개 자동 캡처는 `viewport.height=900` + `full-page=false` 정책 사용 (hero 캡처) — [[1-3-2. 이미지 출처]] §2 |
| **D. 3D 아이콘** | ✅ `apply3DIcon()` (VISUAL_SLOT FIT fill) | ⚠️ 부분 자동 | envelope·calendar·crown·shield·globe·magic-wand 자동 / card·book·chat-bubble·pen·chart-up 미등록 |
| **E. 배경** | ✅ `applyBackground()` (BG_SLOT opacity 20%) + 보조 VISUAL_SLOT | ✅ 완전 자동 | Lexica AI 생성 배경 + Thiings 3D 자동 다운로드 |

> 💡 **수동 imageHash 입력 방식**: 플러그인 UI의 "변형 매트릭스 (JSON 배열)"에 `{"slotId": "...", "imageHash": "...", "name": "..."}` 형식으로 입력하면 모든 유형이 작동.
> 📌 **외부 인프라 요구사항**: A/B/C 유형의 완전 자동화는 Microlink/Playwright API 호출 코드 추가 필요 (외부 서비스 호출은 가능하나 plugin code에 미구현).

---

## 🛠️ use_figma MCP 직접 모드 — 제약사항 (2026-06-12 신규)

> 플러그인이 미지원하는 슬롯 유형(현재 C·E 일부)을 Claude가 use_figma로 직접 빌드할 때 적용. 일반 plugin API와 동작이 다른 부분만 정리.

| API | plugin (정상) | use_figma MCP | 대체 |
|-----|--------------|---------------|------|
| `figma.getNodeById(id)` | ✅ sync | ❌ cross-page 미지원 | **`await figma.getNodeByIdAsync(id)`** |
| `figma.loadAllPagesAsync()` | ✅ | ❌ "not supported" | 제거 — 페이지 순회는 `figma.root.children` 직접 |
| `figma.currentPage = page` | ✅ | ❌ "not supported" | **`await figma.setCurrentPageAsync(page)`** |
| `figma.createImageAsync(url)` | ✅ (manifest 등록 도메인) | ❌ "not supported" | `upload_assets` MCP 도구 + curl POST → response 의 `imageHash` 사용 |
| `fetch(url)` | ✅ manifest 등록 시 | ❌ "fetch is not defined" | 동일 — `upload_assets` 패턴 |
| `throw new Error()` | (변경 적용 후 throw) | ⚠️ 변경 rollback 가능성 — 변경/검증 분리 필요 | 검증은 별도 호출, 변경 함수에서는 throw 회피 |

**자산 업로드 표준 패턴**:
1. PNG 캡처 → vault `.temp/` 폴더 저장
2. `upload_assets` MCP 호출 (count 최대 5) → 단일사용 submitUrl 받기
3. 각 PNG를 `curl -F file=@x.png` 로 POST → response의 `imageHash` 추출
4. 업로드는 부산물로 **임시 frame을 currentPage에 생성** → use_figma 안에서 명시적 `remove()` 필요
5. use_figma 코드 안에서 imageHash를 mockup screen rect의 IMAGE fill로 적용

## 🛠️ 코드 구현 위치 (참고)

- **유형 분류**: `code.js` → `selectLayoutType()`
- **강조어 추출**: `code.js` → `extractEmphasis()`
- **줄 나누기**: `code.js` → `autoLineBreak()` + `countDisplayLen()`
- **fillText 폭 강제**: `code.js` → `textAutoResize=HEIGHT` + `node.resize(350, h)` (06-01)
- **A 슬롯 fill**: `code.js` → `applyMockup()` (CROP+imageTransform)
- **B 슬롯 fill**: `code.js` → `applyEditor()` (EDITOR_SLOT FIT, 06-01 신규)
- **D 슬롯 fill**: `code.js` → `apply3DIcon()` (VISUAL_SLOT FIT)
- **E 슬롯 fill**: `code.js` → `applyBackground()` (BG_SLOT opacity 20% + 보조 VISUAL)
- **데코 매칭**: `code.js` → `selectDecoration()` (§5.4 → §5.1 → sparkle 폴백)
- **viewer 매핑**: `code.js` → `CDBD_VIEWER_MAP` + `selectViewerAssets()` (가이드 §3.1·§2 17개)
- **슬롯 분기**: `code.js` → `createVariantsFromImageData()` (E·A: hasSubtitle 확인)
- **E 이미지 생성**: `ui.html` → `search-openverse` 핸들러 (Lexica.art)
- **A 이미지 캡처**: `ui.html` → `capture-viewers` 핸들러 (Microlink + cdbd.in)
- **B 예시 버튼**: `ui.html` → `fillExampleB` 핸들러 (06-01 신규)

---

## 갱신 로그

| 일자 | 변경 내용 |
|------|-----------|
| 2026-06-01 | 초기 작성 — 오늘 작업 중 강조된 4가지 핵심 항목 + 단계별 체크리스트 정리 |
| 2026-06-01 | **A 유형 완전 자동화 추가** — Microlink + cdbd.in viewer 캡처 통합. 가이드 §3.1·§2 17개 매핑 → `CDBD_VIEWER_MAP`. A 유형 슬롯 분기 (서브 유무): 1:1266/1:1251 또는 1:1269/1:1246. UI에 `capture-viewers` 핸들러 추가. 무료 50 req/day. |
| 2026-06-04 | **최신 결정 반영 (06-01 #2 세션)** — ① TITLE 폭 모든 유형 350 통일 (이전 300/350 혼재) ② 줄 권장 10~12자 (이전 8-10자) ③ fillText 동작 `HEIGHT` + `resize(350)` (이전 `WIDTH_AND_HEIGHT`) ④ B 유형 슬롯 fill ✅ (`applyEditor` 추가) ⑤ 핵심항목 #2 "폭 검수 누락" 사고 사례 추가 ⑥ Step 7에 슬롯 충돌 검증 항목 추가. |
| 2026-06-04 | **텍스트 삽입 절대 원칙 최상단 강조** — 사용자 요청: 텍스트 삽입에서 사고가 가장 많이 남. ① 의미 단위 줄 나누기 ② 어절 중간 자르기 금지 ③ 기본 2-3줄, 이미지 겹치면 4줄 — 3원칙을 체크리스트 최상단 별도 섹션으로 분리. 기존 "자주 놓치는 핵심 항목"의 중복 줄나누기·폭검수 항목 제거. |
| 2026-06-04 | **사전 체크 섹션 (Step 0~6) 전체 제거** — 가이드 문서와 100% 중복이라 단순화. 체크리스트는 "가이드를 디폴트로 깔고 그 위에 더 신경쓸 부분만" 정리하는 역할로 재정의. |
| 2026-06-04 | **작업 중·사후 체크 중복 항목 제거 (Step 7·8·9·11·12·14)** — 가이드와 중복(색상·자산 fill·베리에이션·슬롯 스펙) 또는 최상단 텍스트 절대원칙과 중복(줄나누기·줄바꿈 검증) 항목 모두 제거. |
| 2026-06-04 | **"가이드에 없는 자동화 고유 체크" 섹션도 제거** — 검증 결과 텍스트 y 좌표(`y = 410 - height` [[1-1. 스타일]] §3 / `(450-h)/2` [[1-2-1]] §1.2)도, 시각적 검증(`x ≥ 280` [[1-3-1]] §1.2 / 텍스트 가리지 않기 / 의미 영역 §1.1.b)도 모두 가이드에 명세돼 있음. 체크리스트는 최상단 텍스트 절대원칙 + 자주 놓치는 핵심항목 + 사용자 검수만 남김. |
| 2026-06-04 | **원본 텍스트 변경 금지 원칙 확정 (절대원칙 ①)** — 사고: ① "고객도 지치는," "전화 핑퐁 모바일," 쉼표 임의 추가 → ② 정정 시도에서 "줄 끝 쉼표 제거" 룰을 또 임의로 만듦 → ③ 사용자 정정: **원본 그대로 + `\n` 추가만**. 쉼표·구두점·글자 일체 손대지 말 것. [[1-1. 스타일]] §3에도 동일 룰 명시. |
| 2026-06-12 | **C 유형 첫 시험 완료 + 가이드 5건 갱신** — "코딩 없이 10분 완성! 모바일 페이지 빌더 CdBd"로 C(`1:1257`) 안 2개 자동 생성 (use_figma 직접 모드). 발견 항목: ① mockup ID `1:1368`→`75:34` ([[1-3-1]]) ② TITLE width 350→460 예외 ([[1-1]]) ③ C 레퍼런스 노드 `4648:169` 부재 ([[1-2-2]]) ④ Microlink viewport 정책 — C는 `viewport.height=900`+`full-page=false` ([[1-3-2]]) ⑤ use_figma MCP 환경 async API 제약 — `getNodeByIdAsync`·`setCurrentPageAsync`·`upload_assets` 필수 (이 문서). |
| 2026-06-12 | **서브타이틀 무조건 1줄 룰 추가** — §자주 놓치는 핵심 항목 #4 신설. [[1-1. 스타일]] §3 가이드는 "최대 2줄" 허용이지만 자동화 단계는 더 엄격하게 1줄. width 460까지 확장 허용. 마스터스교회 시안 "2일 연속 예배를 한 페이지에" 사례에서 확인. |
| 2026-06-12 | **노트 간 룰 통합 정리** — Claude가 헷갈리던 5건 정리: ① SUBTITLE 1줄 — [[1-1. 스타일]] §3가 "최대 2줄"에서 "1줄"로 갱신, 자동화/스타일 룰 통합 ② TITLE 강조어 1개 — [[1-1. 스타일]] §3 "보통 1개, 최대 2개" → "항상 1개" ([[3. 검수 체크리스트]] ④와 일치) ③ sparkle 화이트리스트 — sparkle1(`4:141`) 사용 금지 명시를 [[1-3-1. 이미지 규칙]] §1.2 / [[1-3-2. 이미지 출처]] §4.1·§4.3·§5.1 전반에 반영 ④ 옛 마스터 `1:1368` → `75:34` ([[1-3-1. 이미지 규칙]] §1.1.a) ⑤ Figma 페이지 `26:2` 💻 출력 페이지 누락 보강 ([[1-3-2. 이미지 출처]] §4.3). 자주 놓치는 핵심 항목에 #5(강조어 1개)·#6(sparkle 화이트리스트) 추가. |
