---
figma: https://www.figma.com/design/qRFc2HpQ4Mp562LQMB8BX0/블로그-이미지
updated: 2026-06-19
---

# 레이아웃 유형 — 합성 (CdBd 화면 6+α종)

> 🔄 **2026-06-23 현재 템플릿 구성으로 재정의.** (옛 "11유형 A~K" 명칭 폐기 — G·I·J·K 삭제, I 통계는 D에 통합, L~R 슬롯 폐지)

> 레이아웃 = **출력 크기(1920×1200 · 1000×1000) × 구성 유형**. 구성 유형은 **두 갈래(형제)**로 나뉜다:
> - **① 합성 유형 (CdBd 화면)** — 목업·에디터·통계를 슬롯에 채워 만드는 유형. **A·B·C·D·E·F** + 정사각 변형 + **H(sq)**. → **이 문서**
> - **② 외부·실사 (L~R)** — 실사진·외부캡처·인포그래픽·문제신·가격표·콜라주·수치일러스트. **전용 슬롯 폐지 → 콘텐츠 "사전"**(만들 땐 A/C 차용·자유합성). → [[1-2-2. 레이아웃 유형 — 외부·실사 (L~R)]]
>
> 공통 인프라 — **결정 매트릭스(§1) · 슬롯 운영(§2) · 자동화 절차(§4)** — 는 두 갈래 모두에 적용된다.
> ⬆️ 상위: 주제→세트 [[1-0. 주제]] · 노드 인덱스 [[1-2-3. 유형별 레퍼런스]] · 토큰 [[1-1. 스타일]] · 카피 [[1-4. 카피 규칙 (라벨·캡션 텍스트)]] · 컴포넌트 [[1-3. 이미지 규칙]]
>
> **현 템플릿 한눈에** (정식 = v1 코어 + 정사각, §2): A 히어로 · B 비교 · C 목업×4 · D 에디터/통계 · E 목업+에디터 · F 사용법 / 정사각 B·C·D·H. *(v2 자산교체형은 로드맵 — §3.10)*

---

## 1. 결정 매트릭스 — 어떤 유형을 쓸 것인가

> ⚠️ **전제**: 이 매트릭스는 [[1-0. 주제]]에서 **글 주제 → 이미지 세트(유형 시퀀스)**가 정해진 뒤, **개별 이미지 1장의 크기·패턴**을 고르는 단계다. 유형이 L~R(외부·실사)로 결정됐다면 본 매트릭스 대신 [[1-2-2. 레이아웃 유형 — 외부·실사 (L~R)]]을 따른다.
>
> 입력: **본문 한 단락 + "이 자리에 어떤 메시지의 이미지가 필요한가"**
> 출력: **출력 크기 1종 + 구성 패턴 1종**

### Step 1 — 출력 크기 결정

| 문맥 신호 | 선택 |
|---------|------|
| 글의 **메인 시각** / 섹션 도입부 / 비교·시연 핵심 | **1920×1200 (Landscape)** |
| 본문 흐름 중간 **포인트 강조** / 모달·디테일 줌인 / 짧은 보조 시각 | **1000×1000 (Square)** |
| 같은 내용을 가로·세로 두 곳에 모두 보여줘야 할 때 | **두 크기 모두 제작** |

### Step 2 — 구성 유형 결정 (본문 메시지 → 유형)

> **먼저 유형군 분기**: 만들 이미지가 **CdBd 화면(목업·에디터·통계)**인가 → 아래 **① 합성 유형 A~K**. **외부 화면·실사진·개념도·문제신·수치 일러스트**인가 → **② 외부·실사 유형 L~R** ([[1-2-2. 레이아웃 유형 — 외부·실사 (L~R)]]). 주제별로 어떤 유형군을 쓰는지는 [[1-0. 주제]] §2.1 플랜이 이미 지정.

**🟧 1920×1200 (Landscape) — 합성 유형 6가지 (A~F)**

| 유형                        | 신호 (본문이 무엇을 말하는가)                        | 시각 구성                            |
| ------------------------- | ---------------------------------------- | -------------------------------- |
| **A. 히어로 이미지 (목업)**       | 글의 도입부·결론·메인 비주얼. "X라는 제품/페이지를 소개"       | **3개 정자세 폰**이 가로 일렬로 배치 (중앙 폰 -128px elevation). 각 화면에 viewer 캡처 (서로 다른 섹션 or vertical scroll position) ([[#3.1 A. 히어로 이미지 (목업) — `1:12129` ⭐ (2026-06-19 갱신)\|§3.1]]) |
| **B. 비교 (vs)**            | "A 대신 B" / "기존 vs 신규" / "PDF vs 디지털"     | 좌·우 분할 — 각 사이드에 라벨 pill + 아이콘·캡처 |
| **C. 목업 + 목업**            | 같은 viewer의 **다른 페이지** 또는 **같은 기능의 여러 사례** | **폰 4개 (2×2 그리드, 대각선)** ([[#3.3 C. 목업 + 목업 — `15:84` ⭐ (2026-06-19 갱신 — 2×2 패턴)\|§3.3]]) |
| **D. 에디터**                | "**편집 / 관리** 화면에서 일어나는 일" — 빌더·통계·CRM    | 에디터 UI 캡처 단독 (일부는 폰 small inset) |
| **E. 목업 + 에디터**           | "에디터에서 만든 결과 = 폰의 viewer" 인과 관계          | 좌측 에디터 UI + 우측 폰 목업              |
| **F. 사용법 (Step-by-Step)** | 단계적 시연 — "이렇게 → 이렇게 → 이렇게"               | 폰 3~4개를 `>` 화살표로 연결, 단계 라벨       |

**🟦 1000×1000 (Square) — 정사각 변형** *(본문 흐름 중간 포인트·보조 삽입용. G·J·K 삭제됨)*

| 유형 | 신호 | 시각 구성 |
|------|------|----------|
| **H. 목업 + 에디터 (square)** | 폰(결과 화면) + 보조 카드(에디터 발췌·QR·뱃지) | 좌 폰 + 우 화이트 카드 + 상단 라벨 (라운드 프레임) — sq 전용 |
| **B (square)** | 좁은 곳에서 Before/After 대비 | 좌·우 분할 축소판 |
| **C (square)** | 결과물 2개 컴팩트 나열 | 폰 2개 |
| **D (square)** | 에디터·통계 화면 1장 보조 | 풀스크린 단독 (통계는 EMPHASIS) |

> ※ **통계**는 별도 유형이 아니라 **D(에디터/통계)** 가 담당 — 통계 화면일 때 `EMPHASIS_SLOT`로 핵심지표 강조 (구 "I 통계"는 D에 통합).

### Step 3 — 상황 → 템플릿 정밀 선택 (카테고라이징) ⭐

> 템플릿이 압축되며 **한 템플릿이 여러 상황을 겸한다.** 비슷한 후보가 겹칠 때 아래 **4축 → 결정 절차 → 혼동 페어 판별 → 상황 색인** 순으로 좁힌다.

**선택을 가르는 4축**

| 축 | 질문 | 값 |
|---|---|---|
| ① 출처 | 무엇을 **보여주나** | viewer(결과화면) / 에디터·통계 / 대비 / 단계 / 외부·실사·개념(L~R) |
| ② 의도 | 무엇을 **말하나** | 소개 · 나열(다양성) · 대비 · 인과 · 시연 · 성과 · 강조 |
| ③ 크기 | 어디에 **놓이나** | 메인/도입=가로(L) · 본문중간 포인트=정사각(S) |
| ④ 주제 | 글 종류([[1-0. 주제]]) | 세트를 편향(후보 풀) |

**결정 절차 (3단)**

```
1단 — 출처로 유형군 분기
  ├ 모바일 결과화면(viewer)      → A · C · H(좌폰)
  ├ 에디터/관리/통계 화면         → D · E
  ├ 두 방식의 대비               → B
  ├ 1·2·3 단계 흐름             → F
  └ 외부·실사·개념·수치(구 L~R)   → 캡처/실사 후 A·C 차용 or 자유합성
2단 — 의도로 템플릿 확정 (아래 매트릭스)
3단 — 크기 변형 (L 기본 / 본문중간 보조면 정사각 B·C·D·H)
```

**의도 × 출처 매트릭스**

| 의도 | viewer 결과화면 | 에디터/통계 | 대비/외부/개념 |
|------|----------------|------------|---------------|
| **소개·대표** | **A** 히어로(폰3) | — | 실사=실사진 풀스크린 |
| **나열·다양성** | **C** 목업×4 (2개=C-sq) | — | **C**틀에 외부캡처 |
| **대비** | — | — | **B** 비교 |
| **인과**(만들면→나온다) | — | **E** 에디터+결과폰 | — |
| **화면 자체** | — | **D** 단독 | — |
| **성과·데이터** | — | **D**(통계모드+EMPHASIS) | 수치 훅=자유합성 |
| **시연**(단계) | **F** 사용법 | — | — |
| **포인트 강조**(보조) | **H**(폰+카드 sq) | **D-sq** | **B-sq** |

**🔑 혼동 페어 판별 규칙** (가장 헷갈리는 지점)

- **A ↔ C** — A=제품/페이지 **대표 1개** 소개(같은 결과물의 다른 섹션, 폰3) / C=**여러 개**(여러 페이지·여러 사례, 폰4). 개수: **3→A · 4→C · 2→C-sq**.
- **B ↔ 수치 훅** — B=두 **방식/화면**을 좌우 대비(좌우 라벨+자산) / 숫자 **하나**를 크게(0원·N% 절감)=전용 슬롯 없음 → **자유합성**.
- **D ↔ E** — D=화면 **단독**으로 충분 / E=에디터+**결과폰 인과 한 쌍**("만들면→나온다"). 결과폰 동반이면 E.
- **D-에디터 ↔ D-통계** — 같은 틀. 통계/대시보드면 **`EMPHASIS_SLOT`로 핵심지표 확대 필수**(단순 캡처 ❌).
- **L(가로) ↔ S(정사각)** — L=도입/섹션 메인·비교/시연 / S=본문 중간 포인트·짧은 보조.

**구 L~R 라우팅** (전용 슬롯 폐지 후)

| 구 유형 | 지금 |
|---|---|
| L 실사진 | 실사진 소싱 → 풀스크린/A틀 차용 |
| M 외부캡처(경쟁사·툴·인스타) | 캡처 → **C틀(나열)** 또는 단순 배치 |
| N 개념도·O 문제신·R 수치 | **자유합성**(다이어그램/메모지+아이콘/큰 숫자) |
| P 가격표·Q 콜라주 | 캡처 또는 freeform |

**상황별 빠른 색인**

| 상황(본문 메시지) | 템플릿 |
|---|---|
| 제품/페이지 소개·도입 | **A**(L) |
| 결과물 여러 페이지/사례 나열 | **C**(L) · 2개 **C-sq** |
| 기존 방식 vs CdBd 대비 | **B**(L) · 본문중간 **B-sq** |
| 에디터에서 만드는 법(결과까지) | **E**(L) |
| 관리/CRM/설문 화면 자체 | **D**(L) |
| 성과·페이지뷰·통계 | **D**(L, 통계모드+EMPHASIS) |
| 단계별 사용법 1·2·3 | **F**(L) |
| 현장 사진·고객 사례 | 실사진 소싱(A틀) |
| 경쟁사·외부 툴 화면 | 캡처→**C**/단순 |
| 큰 숫자 훅(0원·N%) | 자유합성(수치) |
| QR 배포·폰+보조카드 | **H**(sq) |
| 가격 플랜 비교표 | 캡처/자유합성(가격표) |

> 📌 **역할 분리**: [[1-0. 주제]] §2.1 = 주제별 **후보 풀** / 본 §1 = 개별 이미지 1장의 **최종 픽**. 파트(divider)별 매핑 원칙은 [[1-0. 주제]] §2.2.

---

## 2. 슬롯 템플릿 (자동화 clone 소스) ⭐

> **단일 기준 = `🧩 11유형 슬롯 템플릿` 페이지 (`15:23`)**. A~K 11개 프레임이 "빈 슬롯 + 명명된 placeholder rect"로 존재하고, 자동화는 `tmpl.clone()` → `findOne(n => n.name === SLOT_NAME)` → fill 로 동작한다.
> 외부·실사 유형 **L~R**의 슬롯 템플릿(같은 페이지 내 `43:xx`)은 → [[1-2-2. 레이아웃 유형 — 외부·실사 (L~R)]] · 노드 인덱스 [[1-2-3. 유형별 레퍼런스]]
> ⛔ **구 카탈로그 폐기**: 과거 "정리본 시안" 묶음(`1:11488`·`1:11733`·`1:12233`·`1:12543`·`1:15003`·`1:16916`·`1:17813`·`1:19148`·`1:19887` 등)은 **clone 소스로 쓰지 않는다.** 모든 유형의 단일 출처는 아래 표(`15:xx`)다.

[🔗 Figma 슬롯 템플릿 페이지 열기](https://www.figma.com/design/qRFc2HpQ4Mp562LQMB8BX0/블로그-이미지?node-id=15-23)

| 유형 | 슬롯 템플릿 노드 ID | 슬롯 이름 (자동화 키) |
|------|------------------|---------------------|
| **A. 히어로 이미지(목업)** | `15:25` | `BG_CANVAS`(`1:10840`) · `MOCKUP_PHONE_1/2/3` (487×1013) · `SCREEN_1/2/3` (456~457×972~983, cr 61.75) |
| **B. 비교** | `15:74` | `BG_CANVAS`(`1:10841` 비교 split) · `LEFT_LABEL_PILL` · `LEFT_ASSET` · `RIGHT_LABEL_PILL` · `RIGHT_ASSET` |
| **C. 목업+목업** | `15:84` | `BG_CANVAS`(`1:10840`) · **`MOCKUP_PHONE_1~4`** (2×2, 390×810) · `SCREEN_1~4` (366×786, cr 49.4) · ⚠️ **LABEL_PILL 없음** · 2026-06-22 이름 유일화(_1상좌·_2상우·_3하좌·_4하우) |
| **D. 에디터 / 통계** | `15:132` | `BG_CANVAS`(`1:10840`) · `EDITOR_CANVAS`(1520×900, 프레임 중앙) · `EMPHASIS_SLOT`(900×260, 통계 핵심지표 확대·팝업) · `LABEL_PILL`(선택) — **에디터·통계 풀스크린 통합** |
| **E. 목업+에디터** | `15:177` | `BG_CANVAS`(`1:10840`) · `LABEL_PILL` · `EDITOR_PANEL` (900×800) · `MOCKUP_PHONE` (463×962) · `SCREEN` (434×933, cr 58.66) |
| **F. 사용법** | `15:224` | `BG_CANVAS`(`1:10840`) · `STEP_PHONE_1/2/3` (369×768) · `STEP_SCREEN_1/2/3` (346×744, cr 46.8) · `STEP_GROUP_3` · **`Vector 2/3`** (SVG 화살표) · `STEP_LABEL_1/2/3` |
| **H. 목업+에디터 (sq)** ⭐ | `15:325` | `BG_CANVAS`(`1:10839`, **프레임 라운드 40**) · `LABEL_PILL`(`#6C4CFF` solid 40px) · `MOCKUP_PHONE`(342×711 @y218 세로중앙) · `SCREEN`(322×691 cr45) · `EDITOR_CARD`(화이트 카드 440×500 @x486 y278) |

**정사각 변형 (1000×1000)** — 본문 중간 보조

| 유형 | 노드 ID | 슬롯 |
|------|--------|------|
| **B (sq)** | `160:806` | `BG_CANVAS` · `LEFT/RIGHT_LABEL_PILL` · `LEFT/RIGHT_ASSET` |
| **C (sq)** | `160:755` | `BG_CANVAS` · `MOCKUP_PHONE_1/2` · `SCREEN_1/2` |
| **D (sq)** | `160:23` | `BG_CANVAS` · `EDITOR_CANVAS` · `EMPHASIS_SLOT` |
| **H (sq)** | `15:325` | (위 H — 정사각 전용) |

> 🗑️ **삭제됨**: G(목업+목업sq `15:278`)·J(모달 `15:420`)·K(부분강조 `15:464`)·구 I(에디터+통계 `15:372`)·구 I통계(`121:23`) — 미사용/통합. 필요 시 재생성.
> 📐 **REF(읽기전용)**: 기존 블로그 원본 7종(`142:23`~`146:23`)은 **clone 소스 아님** — 디자인 참고용. (실제 블로그 룩 확인용)
> 🧪 **v2 자산교체형(로드맵)**: A·B·C·D·E·H의 "실제 블로그 레이아웃 + 자산 swap" 버전(`165:23`·`167:23/52/154/279/424`)이 별도 존재 — **미완성**(F 없음·슬롯명 정규화 필요). 상세·완성 계획 → §3.10.

**Placeholder 시각 약속** (2026-06-19 v3 — 캔버스 컴포넌트 BG 기반)
- **`BG_CANVAS`** = 캔버스 컴포넌트 instance (`✨ 컴포넌트` 페이지 §캔버스 목업 — `1:10840`·`1:10841`·`1:10838`·`1:10839`). **자동화가 건들지 않음**. `locked=true`로 잠겨있음. 그리드 라인은 마스터 컴포넌트 디자인이며 출력 이미지에는 반영 X (Figma 표시 가이드만)
- **실체 PNG fill** (`MOCKUP_PHONE_*`) = 폰 frame 이미지 (`36c4ee...`) — 자동화가 건들지 않음
- **점선 보라 stroke + 회색 fill** = 자동화가 캡처/자산으로 채울 슬롯 (`SCREEN_*`·`EDITOR_*`·`LEFT_ASSET` 등)
- **점선 보라 pill** = 라벨 텍스트 들어갈 자리 — **선택적** (콘텐츠 없으면 `remove()`)

**🆕 라벨 슬롯 처리 룰** (2026-06-19 추가)

> **라벨 슬롯은 들어갈 내용이 없으면 채우지 않아도 된다.**

- `LABEL_PILL` · `LEFT_LABEL_PILL` · `RIGHT_LABEL_PILL` · `STEP_LABEL_*` 등 **모든 라벨류 슬롯은 선택적**
- 본문에서 컨텍스트 라벨이 자연스럽게 도출되지 않으면 → **빈 placeholder 제거 + 텍스트 추가 ❌**
- 자동화 동작: 라벨 콘텐츠 없으면 `pillNode.remove()` 호출 — 깔끔하게 사라지게
- A 유형 TRINTAS 사례처럼 **시각 콘텐츠(폰+캡처)만으로 메시지 전달**이 명확하면 라벨 불필요

```js
// 라벨 슬롯 조건부 처리 패턴
const pillContent = decideLabelText(bodyContext); // 없으면 null
if (pillContent) {
  // 텍스트 노드 만들고 pill 안에 중앙 정렬
  pill.strokes = []; // 점선 stroke 제거
  // ... add text
} else {
  pill.remove(); // 빈 라벨이면 통째로 제거
}
```

**자동화 사용 패턴** (예: A 유형 — 캔버스 BG + 슬롯 fill)
```js
// 1. 슬롯 템플릿 clone → 출력 페이지(1:20182)에 appendChild
const tmpl = await figma.getNodeByIdAsync('15:25');
const out = tmpl.clone();
outputPage.appendChild(out);

// 2. BG_CANVAS는 건들지 않음 (locked, 캔버스 컴포넌트가 BG 제공)

// 3. SCREEN 슬롯에 viewer 캡처 fill (점선 stroke 제거)
const s1 = out.findOne(n => n.name === 'SCREEN_1');
s1.fills = [{type:'IMAGE', imageHash: capHashA, scaleMode:'FILL', imageTransform:[[1,0,0],[0,1,0]]}];
s1.strokes = [];

// 4. 라벨 pill 처리 (선택적)
const pill = out.findOne(n => n.name === 'LABEL_PILL');
if (pill && labelText) {
  pill.strokes = [];
  // ... text node 추가
} else if (pill) {
  pill.remove();
}
```

**✅ 캡처 fill 실전 절차 (2026-06-22 A유형 end-to-end 검증)**

> ⚠️ **`upload_assets`의 `nodeId` 직접 배치는 슬롯에 fill되지 않았다** (SOLID 유지). 검증된 2-스텝 방식을 쓴다:

1. **이미지 업로드 → imageHash 확보**: `upload_assets`(count=N, nodeId 없이) 로 단일사용 URL N개 받기 → 각 URL에 PNG를 `curl -F "file=@cap.png;type=image/png"` POST → 응답의 `imageHash` 수집.
2. **`use_figma`로 슬롯 fill**: clone한 프레임의 `SCREEN_*`(또는 `EDITOR_*`·`ASSET`)에 직접 대입 —
   ```js
   n.fills = [{ type:'IMAGE', imageHash: HASH, scaleMode:'FILL', imageTransform:[[1,0,0],[0,1,0]] }];
   n.strokes = []; // 점선 제거
   ```
   - 캡처 aspect = 슬롯 aspect(A는 0.4654)면 identity transform으로 왜곡 0.
   - **z-order**: `MOCKUP_PHONE_*`(폰 프레임 PNG, 아래) → `SCREEN_*`(캡처, 위) 순서 유지 — clone은 원본 순서를 보존하므로 그대로 두면 됨.
- **출력 페이지**: `💻 AI 블로그 이미지 제작` (`1:20182`). clone을 이 페이지에 append.

**캔버스 BG 룰** (2026-06-19 v3)
- 각 layout의 첫 자식은 `BG_CANVAS` — `✨ 컴포넌트` 페이지의 캔버스 컴포넌트(`1:10840` · `1:10841` · `1:10838` · `1:10839`) instance. **`locked=true`, 자동화 건들지 않음**
- BG 디자인을 바꾸려면: **캔버스 마스터 컴포넌트만 수정** → 11개 layout 모두 자동 전파
- 자동화가 새 layout 만들 때: clone 후 BG_CANVAS는 그대로 두고 다른 슬롯만 fill
- 캔버스 컴포넌트의 그리드 라인은 Figma 표시 가이드용 — **출력 이미지에는 안 보임** (마스터 디자인에서 export 제외 처리)

> ⚠️ **유형별 정답 ref 검증 상태** (2026-06-19 사용자 수정 후 갱신):
> - ✅ **A** — `15:25` 정답 검증 완료. 사용자가 폰/스크린 사이즈를 487×1013, cr 61.75로 조정.
> - ✅ **C** — 사용자가 **2×2 그리드 4개 폰 패턴**으로 확정. LABEL_PILL 제거.
> - ✅ **F** — ARROW placeholder를 실제 SVG 화살표(`Vector 2/3`)로 교체.
> - ✅ **G·H·I** — 사용자가 좌표 미세 조정 + 라벨 슬롯 제거 (H·I).
> - **라벨 슬롯 보유**: B·E·F (B만 좌·우 2개) — **D는 2026-06-22 라벨 제거**
> - **라벨 슬롯 없음**: A·C·G·H·I·J·K — 시각 자체로 컨텍스트 명확

---

## 3. 유형별 상세 룰

### 3.1. A. 히어로 이미지 (목업) — `1:12129` ⭐ (2026-06-19 갱신)

> 글의 도입부·결론을 장식하는 **메인 비주얼**. 본문이 무엇을 말하는지 한눈에.

**🔴 정답 레퍼런스**: `1:12129` (채워진 검증 예시) · **clone 소스 = `15:25`** (§2 슬롯 템플릿)
- **이전 패턴 폐기** (`1:11487` 히어로 목업 컴포넌트의 V자 기울임 페어) — 화면 fill 시 컨텐츠가 기이하게 잘림. 사용 ❌.

**레이아웃 — 3개 폰 정자세 + 중앙 elevation**

**고정 좌표 (절대값)** — 2026-06-19 사용자 수정 후 실측 값

| 슬롯 | x | y | w | h | cornerRadius | 비고 |
|------|---|---|---|---|--------------|------|
| **MOCKUP_PHONE_1** (좌측 phone frame) | 162 | 267 | 487 | 1013 | 0 | IMAGE fill, hash `36c4ee3ec8127bb777216967bbf30a447ed93905` (공식 폰 프레임 PNG) |
| **MOCKUP_PHONE_2** (중앙 phone frame) | 716 | 119 | 487 | 1013 | 0 | 같은 hash. 중앙은 y -148 elevation |
| **MOCKUP_PHONE_3** (우측 phone frame) | 1270 | 267 | 487 | 1013 | 0 | 같은 hash |
| **SCREEN_1** (좌측 화면) | 177 | 280 | 457 | 982 | 61.75 | viewer 캡처 hash A |
| **SCREEN_2** (중앙 화면) | 731 | 133 | 456 | 983 | 61.75 | viewer 캡처 hash B |
| **SCREEN_3** (우측 화면) | 1286 | 282 | 456 | 972 | 61.75 | viewer 캡처 hash C |

- **위·아래 폰 padding**: 폰 frame 내부 ~16px (left/top), ~32px (bottom — phone bottom bezel)
- **폰 간 가로 간격**: 70px (1286-703-513, 703-120-513)

**캡처 자산**

- **screen aspect** = 457/982 ≈ **0.4654** (사용자 수정 후 실측, A 유형 SCREEN_1 기준)
- viewer fullpage 캡처 → PIL로 약 **750×1611** crop 3개

🔴 **캡처 룰 — [[1-3. 이미지 규칙]] §4.1 "사람이 보는 것처럼 분석 후 섹션 분할 캡처" 절대 원칙 준수**

- 3개 폰에 같은 캡처 복붙 ❌
- viewer를 사람처럼 읽어서 **3개 의미 섹션** 선정 → 각 폰에 다른 섹션 fill
- 자세한 절차·예시·fallback → [[1-3. 이미지 규칙]] §4.1

**fill 모드**
- screen rect 모두 `scaleMode: 'FILL'` + identity `imageTransform` 권장 (crop이 정확한 aspect면 distortion 0)
- crop aspect와 screen aspect 차이가 있으면 `imageTransform` Y scale로 미세 조정 (레퍼런스 1:12129도 일부 screen에 0.93~1.01 사이 Y 미세 조정)

**스타일 디테일**
- **글로우 / 그림자 / DECO 모두 없음** — 폰 frame PNG에 그림자 베이크 인. 추가 effect 노이즈.
- **라벨 pill 없음** — 화면 콘텐츠 자체가 컨텍스트 전달

**제작 절차 요약**
1. 본문 키워드 → viewer 슬러그 결정 ([[1-3. 이미지 규칙]] §4.1, 썸네일 §2 카탈로그)
2. viewer 풀페이지 캡처 (thum.io width 750 / wait 20 / `fullpage`)
3. 3개 crop 750×1616 — 의미 섹션 분할 or vertical scroll position
4. `upload_assets` count=3 → 3개 imageHash 확보
5. 1920×1200 frame + 3 phone frames + 3 screens 위 좌표대로 배치 (코드: `code.js` `buildHeroBlog()` 추후 작성)

### 3.2. B. 비교 — `15:74` ⭐ (2026-06-22 갱신 — 실측 레퍼런스 `1:8211`)

> "기존 vs 신규" 명확한 대비를 시각화. 추석 카탈로그 #3에서 검증한 **표준 레이아웃**.

**🔴 정답 레퍼런스**: `1:8211` (좌 종이/PDF · 우 모바일 3폰). 자동화는 이걸 clone → 라벨·sub·특징 텍스트만 본문에 맞게 교체.

**레이아웃 — 좌·우 50:50 분할 (1920×1200)**

| 영역 | 구성 (위→아래) | 비고 |
|------|---------------|------|
| **좌 (Before)** | sub-line → **라벨 pill(무채색)** → 비유 3D 오브젝트(종이 더미 등) → **특징 3줄** | 회색 그라데이션 ellipse 배경 |
| **우 (After)** | sub-line → **라벨 pill(`#6C4CFF`)** → **폰 3대**(엇갈림 배치, 멀티페이지) → **특징 3줄** | 화이트 배경 (가치 우위) |

**텍스트 위계 (실측, Pretendard)**
| 요소 | 폰트/크기 | 예시 |
|------|----------|------|
| 라벨 pill | **Bold 50** | "종이 카탈로그·PDF" / "모바일 카탈로그" |
| sub-line(pill 위) | **SemiBold 36** | "비용은 높고 수정은 어려운" / "낮은 비용으로 데이터 수집까지" |
| 특징 리스트 | **Bold 45** · 3줄 · 끝에 이모지 | "가격·구성 변경 0원 🪄 / 링크·QR 한 줄 배포 ✨ / 페이지뷰·링크별 데이터 📊" |

**원칙**
- **특징 텍스트 = 본문에서 추출** — 레퍼런스 문구 그대로 두지 말고 글의 실제 대비점으로 교체(좌=한계, 우=이점, 각 3줄 대칭).
- **자산 비대칭**: 좌는 **비유 3D 오브젝트**(종이 더미·자물쇠·PDF 아이콘), 우는 **실제 제품 폰 3대**(멀티페이지 목업 `98:2643`, 로고 보존) — "추상적 과거 vs 구체적 현재" 대비.
- **컬러 위계**: 좌 무채색·종이 톤 / 우 퍼플 `#6C4CFF` 강조.
- 특징 텍스트 기준 **45px**(Bold) — 너무 작으면 ❌, 좌·우 동일 크기로 대칭 유지.

### 3.3. C. 목업 + 목업 — `15:84` ⭐ (2026-06-19 갱신 — 2×2 패턴)

> 같은 viewer의 다른 페이지를 나란히 보여주거나, 같은 기능의 여러 사례를 한 번에.

**🔴 정답 레퍼런스**: `15:84` (슬롯 템플릿 페이지 내)

**레이아웃 — 폰 4개 (2×2 그리드, 대각선 배치)**

| 슬롯 | x | y | w | h | 비고 |
|------|---|---|---|---|------|
| **MOCKUP_PHONE_1** (좌상) | 120 | 74 | 390 | 810 | 폰 PNG fill |
| **SCREEN_1** (좌상 화면) | 132 | 85 | 366 | 786 | cr 49.4 |
| **MOCKUP_PHONE_2** (좌상 오른쪽) | 550 | 74 | 390 | 810 | |
| **SCREEN_2** (좌상 오른쪽 화면) | 562 | 85 | 366 | 786 | cr 49.4 |
| **MOCKUP_PHONE_1** (우하) ⚠️ 이름 중복 | 980 | 468 | 390 | 810 | 동명 슬롯 |
| **SCREEN_1** (우하 화면) | 992 | 479 | 366 | 786 | |
| **MOCKUP_PHONE_2** (우하 오른쪽) ⚠️ 이름 중복 | 1410 | 468 | 390 | 810 | |
| **SCREEN_2** (우하 오른쪽 화면) | 1422 | 479 | 366 | 786 | |

- **LABEL_PILL 없음** — C는 시각 자체가 컨텍스트 전달
- **각 폰의 화면**: 같은 viewer의 4개 다른 페이지 캡처 권장
- ⚠️ **자동화 주의**: `findOne(name)`이 첫 매치만 반환하므로 4개 폰 각각 fill하려면 `findAll(name)` 또는 위치 기반 식별 필요
- **A 유형과의 차이**: A는 3개 폰의 hero/scroll progression / C는 4개 폰의 페이지 시리즈

### 3.4. D. 에디터 — `15:132`

> 빌더·CRM·통계 등 **에디터 화면 자체**가 주인공.

- **에디터 캔버스**: `EDITOR_CANVAS` **프레임 중앙 배치**(x=200·y=150). **라벨 없음** — 에디터 화면 자체가 컨텍스트.
- 🎯 **초점 캡처**: 풀 에디터 3분할 덤프 ❌ → 보여줄 **의미 영역(특정 패널/카드)** 으로 크롭하거나 해당 컴포넌트만 사용 ([[2. 검수 체크리스트]] A)
- 포커스 요소가 portrait면 캔버스 중앙에 두고 여백 허용 (컴포넌트는 `rescale` 말고 **원본 크기** — 텍스트 파손 방지)
- **데코**: `✨ sparkle` 1~2개 — 새 기능·강조 영역 옆
- **부분 하이라이트**: 강조 UI 영역을 퍼플 outline 또는 sparkle로 표시

### 3.5. E. 목업 + 에디터 — `15:177`

> "에디터에서 만든 결과 = 폰" 인과 관계 시각화.

- **레이아웃**: 좌측 에디터 패널(약 60%) + 우측 폰 목업(약 40%)
- **연결 신호**: 화살표 `→` 또는 폰을 에디터 위에 살짝 겹치기
- **라벨 pill**: 상단 — 무엇을 만드는 중인지
- **B 유형(비교)와의 차이**: E는 인과(만들기 → 결과), B는 비교(A vs B)

### 3.6. F. 사용법 — `15:224` ⭐ (2026-06-19 갱신)

> "이렇게 → 이렇게 → 이렇게" 단계별 시연.

- **단계 수**: **3개 또는 4개** (5개 이상은 ❌ — 시각 과밀)
- **레이아웃**: 가로 일렬 — `폰 ① > 폰 ② > 폰 ③`
- **폰 사이즈**: 369×768 (`STEP_PHONE_1/2/3`), 화면 346×744 cr 46.8 (`STEP_SCREEN_1/2/3`)
- **화살표**: `Vector 2` · `Vector 3` (SVG, 50×77, x=701/1271, y=506) — 점선 placeholder 아니고 **실제 SVG 그래픽**. 자동화는 건들지 않음.
- **단계 라벨**: `STEP_LABEL_1/2/3` (각 폰 하단 250×60 pill) — 선택적
- **각 폰 화면**: 단계별 viewer 상태 캡처 (실제 클릭 흐름)

### 3.7. H. 목업+에디터 (square) — `15:325` ⭐ (2026-06-22 갱신 — 폰 + 카드 좌우 2분할)

> 좁은 정사각에 **폰(결과 화면) + 카드(에디터·QR·뱃지 등 보조 정보)** 를 좌우로 나란히. "박람회 QR 배포" 등에서 검증.

**레이아웃 — 라운드 프레임(cr 40) · 상단 라벨 · 좌 폰 / 우 카드**

| 슬롯 | x | y | w | h | 비고 |
|------|---|---|---|---|------|
| **프레임** | 0 | 0 | 1000 | 1000 | **cornerRadius 40**(카드형) · `clipsContent` |
| **LABEL_PILL** | 중앙 | 72 | 텍스트+60 | 78 | `#6C4CFF` solid · 흰 텍스트 **SemiBold 40** · cr 100 (선택적, 없으면 `remove()`) |
| **MOCKUP_PHONE** | 75 | 218 | 342 | 711 | 폰 PNG · **세로 중앙**(라벨 아래) |
| **SCREEN** | 86 | 230 | 322 | 691 | cr 45 · 캡처 fill(aspect 0.466) |
| **EDITOR_CARD** | 486 | 278 | 440 | 500 | **화이트 카드**: cr 28 · stroke `#E3DBFF` 2 · 그림자(y12 r32 퍼플 12%) |

**EDITOR_CARD 운영 — 화이트 카드 홀더**
- 풀 에디터 ❌ — 카드 1개·패널 일부·**QR·뱃지** 같은 보조 정보를 담는 **화이트 카드 컨테이너**.
- IMAGE로 채우거나(에디터 카드 발췌), 안에 오브젝트를 올림(QR 등).

**QR 배포 변형 (검증 사례 `115:23`)**
- EDITOR_CARD 안에: **QR 정사각**(280×280, 카드 중앙 상단 +70) FILL + **캡션 2줄**(Bold 34 `#1A1A1F` "현장에서 QR 스캔" / SemiBold 26 `#6B4DFF` "→ 모바일 카탈로그 바로 열람", 카드폭 중앙정렬).
- 좌 폰 SCREEN = 배포 대상 화면(예: 카탈로그 viewer, 로고+네비게이터 보존), LABEL_PILL = "박람회 부스 QR 배포".

### 3.8. I. 통계 (페이지 통계 대시보드 + 강조) — `121:23` ⭐ (2026-06-22, 레퍼런스 `81:774`)

> CdBd **페이지 통계 대시보드**(페이지뷰·클릭수·클릭률 + 라인차트)를 **단독 중앙** 배치 + 상단 **라벨 알약** + **핵심 지표 강조 영역**. "영업 데이터·성과·지표·통계" 주제.
> (구 I.에디터+통계 `15:372`는 2026-06-22 삭제 — 이 통계 단독으로 통합)

**캔버스**: `1920 × 1200`

| 영역 | 슬롯 이름 | 좌표 (x/y) | 크기 (w×h) | 정렬 | 사양 |
|------|----------|-----------|-----------|------|------|
| **배경** | `BG_CANVAS` | 0 / 0 | 1920×1200 | — | 캔버스 컴포넌트 instance, `locked=true` |
| **텍스트(라벨)** | `LABEL_PILL` | 816 / 119 | 288×90 | **상단 중앙** (cx=960) | 보라 알약 `#6C4CFF` · rounded 100 · padding `30/15` · 자동 폭 / 텍스트 **Pretendard SemiBold 50px 흰색** 1줄 |
| **이미지(통계 베이스)** | `STAT_SLOT` | 403 / 269 | 1114×812 | **중앙** (cx=960) | 페이지 통계 컴포넌트 — 3카드 + 라인차트 (베이스) |
| **강조 영역** ⭐ | `EMPHASIS_SLOT` | 181 / 331 | 936×259 | 좌·상 | **핵심 지표 카드(페이지뷰·클릭수)를 확대·팝업**한 클립 프레임 — 흰 카드 + 그림자로 떠 보임 |

**🆕 강조 영역 (2026-06-22 사용자 피드백)**
- 통계 대시보드 **단순 캡처만 하면 어느 수치가 핵심인지 안 보임** → `EMPHASIS_SLOT`으로 **핵심 카드 확대·팝업**시켜 시선 유도
- 구현: 통계 컴포넌트 instance를 **클리핑 프레임(936×259)** 안에서 확대(`scale≈1.44`) 배치 → 상단 2카드만 노출 → 베이스 위에 겹침

**영역 흐름**: 상단 라벨(y119~209) → 강조 카드(y331~590) → 베이스 대시보드(y269~1081, 강조 뒤)

**텍스트 사이즈** (1920×1200): 라벨 SemiBold **50px** / 통계 카드 숫자는 **컴포넌트 자체 사양** (수정 불필요)

**제작**: 슬롯 clone → `LABEL_PILL` 텍스트 교체 → `STAT_SLOT`에 통계 컴포넌트(원본 크기, `rescale` ❌) → `EMPHASIS_SLOT` 내부 통계 확대본 위치 조정(강조할 카드 노출)
> ⚠️ `EDITOR_CANVAS`(hidden 레거시) 사용 X

> 🗑️ **2026-06-22 삭제 유형**: G(목업+목업 sq)·J(모달창)·K(부분강조) — 기존 게시물에서 거의 미사용. 필요해지면 슬롯 재생성 후 본 §3에 재정의.

---

## 4. 자동화 적용 절차 (MCP `use_figma`)

> Claude가 본문을 받았을 때 따르는 흐름. (자동화 체크리스트 별도 작성 예정)

1. **본문 분석** — 메시지 핵심 1줄 + 어떤 종류의 시각이 적합한지 (§1 매트릭스)
2. **출력 크기 결정** (Step 1)
3. **구성 패턴 결정** (Step 2 — 11유형 중 1개)
4. **슬롯 템플릿 clone** — §2 표의 해당 유형 프레임(`15:xx`)을 자동화 출력 페이지로
5. **자산 fill**:
   - 목업 슬롯 → viewer 캡처(의미 영역 crop) + mockup instance
   - 에디터 슬롯 → 에디터 캡처 (PIL crop)
   - 라벨 pill → 컨텍스트 텍스트 (5~12자)
   - 데코 → sparkle 컴포넌트 instance
6. **마무리** — 슬롯 placeholder 제거 + 정렬 확인

---

## 갱신 로그

| 일자 | 변경 내용 |
|------|-----------|
| 2026-06-19 | 최초 작성. Figma 파일 `qRFc2HpQ4Mp562LQMB8BX0` 메타데이터·스크린샷 분석 기반. 출력 크기 2종 × 구성 패턴 11종 정의. 각 유형의 Figma 섹션 ID·슬롯 구성 매핑. |
| 2026-06-19 | **A. 히어로 이미지 패턴 전면 교체** — 이전 `1:11487`의 V자 기울임 페어(`히어로 목업` 컴포넌트)는 vector mask 기반 fill이 캡처 콘텐츠를 기이하게 자르는 문제 발견. 사용자 지시로 **3개 정자세 폰 + 중앙 elevation 패턴** (정답 ref `1:12129`)으로 교체. mockup 1/2/3은 공식 폰 프레임 PNG(`36c4ee...`) 공유, screen 1/2/3은 viewer 캡처 3종. crop 전략(섹션 분할 vs vertical scroll)·고정 좌표·캡처 절차 포함. §2.1 표 및 §3.1 본문 동기화. 실증: TRINTAS · THE SIGNATURE DAY 비공개 기업 행사 RSVP 초대장 샘플 (`12:23`) 제작. |
| 2026-06-19 | **§2.3 신설: 11유형 슬롯 템플릿 페이지** (`🧩 11유형 슬롯 템플릿`, `15:23`) — 11개 깔끔 frame을 새 페이지에 한 번에 생성. 각 frame에 명명된 placeholder slot rect(`SCREEN_*`·`LABEL_PILL`·`EDITOR_CANVAS` 등) 배치. 자동화는 `tmpl.clone()` → `findOne(name)` → fill 패턴으로 사용. A는 1:12129 검증 완료, B~K는 초안 상태로 후속 검증 대기. |
| 2026-06-19 | **§2.3 라벨 슬롯 처리 룰 추가** — `LABEL_PILL` 등 모든 라벨류 슬롯은 **선택적**. 본문에서 자연스러운 컨텍스트 라벨이 안 도출되면 채우지 않고 `pill.remove()` 처리. A TRINTAS 사례처럼 시각 콘텐츠만으로 메시지 전달이 명확하면 라벨 불필요. 자동화 분기 코드 패턴 포함. |
| 2026-06-19 | **§2.3 슬롯 템플릿 v2 (폐기)** — 컴포넌트 instance를 너무 많이 적용한 시도. 의도와 달라 v3로 교체. |
| 2026-06-19 | **§2.3 슬롯 템플릿 v3 — 캔버스 컴포넌트 BG로 단순화** (페이지 `15:23`, 옛 `13:23`·`14:23` 폐기). v1의 깔끔한 슬롯 구조(`SCREEN_*`·`MOCKUP_PHONE_*`·`LABEL_PILL` bare rects) 복원 + **BG만 캔버스 컴포넌트 instance로 교체** (`1:10840` 1920×1200 / `1:10841` 비교 split / `1:10838` 1000×1000 라벤더 / `1:10839` 1000×1000 화이트). 각 layout의 첫 자식 `BG_CANVAS` 노드는 `locked=true`. BG 디자인을 캔버스 마스터에서 수정하면 11개 layout 자동 전파. |
| 2026-06-19 | **§2.3·§3 사용자 manual 조정 후 동기화** — 사용자가 Figma에서 직접 슬롯 좌표/사이즈 미세조정 + 불필요한 라벨 슬롯 제거. 반영 사항: ① **A**: 폰 487×1013, screen 456~457×972~983 (cr 61.75) + 위치 좌표 ② **C**: 2×2 그리드 **4개 폰 패턴**으로 확장 (LABEL_PILL 제거) ③ **E**: 폰 463×962 (cr 58.66) ④ **F**: ARROW placeholder → **실제 SVG 화살표** `Vector 2/3` 교체 ⑤ **G**: 중앙 phone elevation 강화 (y 160) ⑥ **H·I**: LABEL_PILL 제거 + 위치 조정. ⑦ §2.3 표·§3.1·§3.3·§3.6 본문·§1 Step 2 표 동기화. 라벨 슬롯 보유: B·D·E·F (그 외 7개 유형은 시각 자체로 컨텍스트 명확). |
| 2026-06-22 | **상위 주제 계층 + 외부·실사 유형 연결** — 블로그 15페이지 390장 분석으로 이 문서의 11유형(A~K)이 **CdBd 목업·에디터 중심**이라 사례·소트리더십·라운드업 글을 못 잡는 공백 확인. ① 문서 머리말·§1 전제에 상위 단계 [[1-0. 주제]]와 신규 [[1-2-2. 레이아웃 유형 — 외부·실사 (L~R)]](L~R) 링크 추가. ② 본 문서 범위를 "A~K 합성 유형"으로 명확화. (좌표·슬롯 사양은 변경 없음) |
| 2026-06-22 | **§2 구/신 카탈로그 혼재 정리** — 기존 §2가 옛 정리본 카탈로그(§2.1·§2.2, 구 노드 `1:11488`·`1:11733`…)와 신규 슬롯 템플릿(§2.3)로 이원화돼 혼란스럽던 것을 **슬롯 템플릿 페이지(`15:23`) 단일 기준**으로 통합. 옛 카탈로그 표 제거(구 ID는 "폐기" 안내문으로만 잔존). §3 유형 상세 헤더의 옛 ID(B·D·E·G·H·I·J·K)를 슬롯 템플릿 ID(`15:74`~`15:464`)로 일괄 교체, A는 clone 소스 `15:25` 명시. §4 4단계 문구를 "슬롯 템플릿 clone"으로 정정. + `1-0` 파일명 단축(`1-0. 주제`) 링크 동기화. |
| 2026-06-22 | **유형군 동등 형제 재구성** — L~R도 레이아웃 유형이므로, 머리말·§1을 "구성 유형 = ① 합성 유형(A~K, 이 문서) + ② 외부·실사 유형(L~R, 형제 문서)" 2갈래로 재서술하고 §1 Step2에 유형군 분기 가이드 추가. 파일 번호 재배치: `외부·실사`(구 1-2-3) → **`1-2-2. 외부·실사 유형 (L~R)`**, `유형별 레퍼런스`(구 1-2-2) → **`1-2-3`**. 두 유형 카탈로그(1-2-1·1-2-2) 인접 + 레퍼런스 인덱스(1-2-3) 후미. 전 문서 링크 동기화. |
| 2026-06-22 | **D 템플릿 정리 + 캡처 의도 원칙 + 검수 체크리스트** — 사용자 피드백 반영: ① **D 템플릿(`15:132`)**: `EDITOR_CANVAS` 프레임 세로 중앙(y=150) + **LABEL_PILL 슬롯 제거**(D는 라벨 없음). ② **캡처 의도(초점)** 원칙 — 풀 에디터 3분할 덤프 ❌, 의미 영역 크롭/컴포넌트만 사용(예: D RSVP=질문과답변 폼, E=카드 빌더 크롭). ③ **`rescale()`이 컴포넌트 텍스트 파손** → 원본 크기 배치. ④ 신규 [[2. 검수 체크리스트]] 작성(사용자 제시 전 A~H 게이트). 영향 이미지(58:1148 통계 중앙·숫자복구 / 66:348 D RSVP / 66:392 E) 재작업. |
| 2026-06-22 | **⭐ 풀세트 5장 자동 생성 검증** — "기업 행사 초대장 5분 만들기"(가이드+시리즈) 1글 → 빌드플랜 → A(seminar 3섹션)·C(행사종류 4 viewer)·D(Q&A 에디터캡처)·E(에디터+결과폰)·I(페이지통계) 5장 생성. **검증된 fill 경로 3종**: ① viewer 캡처 → `SCREEN_*` ② 에디터 캡처 PNG → `EDITOR_CANVAS`/`EDITOR_PANEL` ③ 컴포넌트 인스턴스(페이지통계). C의 `SCREEN_*` 중복명은 위치(y,x)순 정렬로 4개 개별 fill. viewer 캡처는 공개 페이지라 로그인 불필요(`capture-seminar-3sections.mjs <url> <prefix>`); buttery_moment/opening/wedding 등 일부 슬러그는 빈 렌더(유효 invitation viewer = seminar·personalized·rsvp). |
| 2026-06-22 | **⭐ A유형 fill 파이프라인 end-to-end 검증 (첫 자동 생성)** — 출력 페이지 `💻 AI 블로그 이미지 제작`(`1:20182`)에 A 템플릿(`15:25`) clone → seminar viewer 3섹션 캡처(hero/RSVP/contact, 780×1676=0.4654)를 `SCREEN_1/2/3`에 fill → 1920×1200 히어로 이미지 완성. **핵심 학습**: `upload_assets`의 `nodeId` 직접 배치는 fill 안 됨 → "업로드로 imageHash 확보 → `use_figma`로 `n.fills` 대입" 2-스텝이 정답. §2에 "✅ 캡처 fill 실전 절차" 추가. (캡처 인프라 `cdbd-capture/`, 출력 노드 `49:63`) |
| 2026-06-22 | **B 비교 §3.2 실측 갱신** — 추석 카탈로그 #3 B(`101:984`) 기준으로 좌 종이 3D/우 폰3대, 라벨 50·sub 36·특징 **45px**(사용자 조절), 특징=본문 추출 원칙 명문화(레퍼런스 `1:8211`). |
| 2026-06-22 | **H 목업+에디터(sq) §3.8·§2 재정의** — 사용자가 Part5(`115:23`)를 직접 다듬은 결과를 슬롯 템플릿 `15:325`에 반영: ① **프레임 라운드 40**(카드형) ② **폰 축소·세로 중앙**(342×711 @y218, screen 322×691 cr45) ③ **EDITOR_CARD = 화이트 카드 홀더**(cr28 + stroke `#E3DBFF` + 그림자, @x486 y278) ④ **LABEL_PILL 신규**(`#6C4CFF` solid, SemiBold 40, cr100, 상단 중앙) — 기존 H의 "LABEL_PILL 없음" 폐기. §3.8에 QR 배포 변형(QR 280² + 캡션 2줄) 문서화. |
