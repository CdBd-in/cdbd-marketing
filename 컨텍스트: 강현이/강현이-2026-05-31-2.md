# Session Context: 강현이 (2026-05-31 #2 — vault 구조 정리 + 폴더명 한글화)

**Date:** 2026-05-31 (2번째 세션)
**이전 세션:** [[컨텍스트: 강현이/강현이-2026-05-31]] (Figma 플러그인 자동화 + D 유형) · [[컨텍스트: 강현이/강현이-2026-05-29-2]] · [[컨텍스트: 강현이/강현이-2026-05-29]]
**Status:** ✅ 정리 완료 — vault 구조 한글 일관성 + 도메인 응집성 확보 + 자동화 한계 문서화

> **이 문서의 역할**: 이번 세션은 vault **구조 정리** 위주. 자동화 코드 변경은 거의 없고 폴더 재배치·문서 갱신·outdated 자료 제거가 핵심.

---

## ⚡ TL;DR

**이번 세션 (12 tasks 완료)** = vault 구조를 한글 일관성으로 정리하고 도메인 응집성 확보.

- ✅ A 유형 상단 슬롯 y값 46 반영 (1:1266/1269)
- ✅ `1-4. 프롬프트.md` 삭제 — intake 패턴은 [[0. 목표와 비전]] §파이프라인으로 통합
- ✅ orphan 폴더 3건 제거 (`블로그/썸네일/attachments/`, `screenshots/`, `1. 블로그/`)
- ✅ `attachments/decorations/` → 도메인 안으로 이동 (`1-3-3. 이미지 자산/데코/`)
- ✅ `scripts/` → `자동화 도구/` rename (한글 일관성)
- ✅ `decorations/` → `데코/` rename
- ✅ [[0. 목표와 비전]]에 "현재 구조의 한계" 섹션 추가 (왜 플러그인 단계가 끼어있나)
- ✅ 모든 코드·JSON·문서·gitignore 경로 일괄 갱신 + `auto-fetch-decoration.py list`로 동작 검증

**검증 완료**: P1 envelope D 유형 4안 — 사용자가 26:2 페이지에서 잘 떴다고 확인.

---

## 📂 핵심 파일 위치 (이번 세션 경로 변경 반영)

### 자동화 코드 (rename: scripts → 자동화 도구)
| 파일 | 역할 |
|------|------|
| `자동화 도구/cdbd-thumbnail-plugin/code.js` | Figma 플러그인 메인 |
| `자동화 도구/cdbd-thumbnail-plugin/ui.html` | 플러그인 UI |
| `자동화 도구/cdbd-thumbnail-plugin/manifest.json` | 매니페스트 |
| `자동화 도구/cdbd-thumbnail-plugin/decorations.json` | 자산 메타 카탈로그 |
| `자동화 도구/cdbd-thumbnail-plugin/auto-fetch-decoration.py` | Thiings 자동 다운로드 |
| `자동화 도구/cdbd-capture/` | Playwright 캡쳐 인프라 |

### 데코 이미지 자산 (이동: attachments/decorations → 1-3-3/데코)
`블로그/썸네일/1. 디자인 가이드/1-3. 이미지/1-3-3. 이미지 자산/데코/`
- uploaded: sparkle, shield, envelope, credit-card (Figma hash 있음 — **백업**)
- downloaded: crown, calendar, globe (Figma 미등록 — **source**)

### 문서 (디자인 가이드)
| 파일 | 이번 세션 변경 |
|------|--------------|
| [[0. 목표와 비전]] | intake 패턴 inline 통합 / 3개 기둥 표 정리 / "현재 구조의 한계" 신규 |
| [[1-2-1. 레이아웃 규칙]] | §3.1 A형 상단 슬롯 y68 → **y46** |
| [[1-3-1. 이미지 규칙]] | 데코 경로 갱신 + 1-4 참조 제거 |
| [[1-3-2. 이미지 출처]] | 경로 일괄 갱신 (decorations→데코 / scripts→자동화 도구) + 1-4 참조 제거 |
| ~~`1-4. 프롬프트.md`~~ | **삭제** (자동화로 의미 상실, intake는 0으로 통합) |

---

## 🗂️ 최종 vault 구조

```
cdbd-marketing/
├── 블로그/
│   └── 썸네일/
│       ├── 0. 목표와 비전.md
│       └── 1. 디자인 가이드/
│           ├── 1-1. 스타일.md
│           ├── 1-2. 레이아웃/
│           │   ├── 1-2-1. 레이아웃 규칙.md
│           │   └── 1-2-2. 레이아웃별 레퍼런스 노드.md
│           └── 1-3. 이미지/
│               ├── 1-3-1. 이미지 규칙.md
│               ├── 1-3-2. 이미지 출처.md
│               └── 1-3-3. 이미지 자산/
│                   └── 데코/ (7개 PNG)
├── 자동화 도구/  ← scripts에서 rename
│   ├── cdbd-thumbnail-plugin/  (Figma 플러그인)
│   └── cdbd-capture/           (Playwright)
├── 컨텍스트: 강현이/
├── 컨텍스트: 이선호/
└── 홈페이지/
```

**제거된 폴더**:
- `블로그/썸네일/attachments/` (17개 orphan PNG, 1.2MB)
- `attachments/captures/` (18개 Microlink 시행착오 PNG, 11MB)
- `screenshots/` (vault 루트 step2-error.png 1개)
- `1. 블로그/` (2026-05-22 옛 디자인 시스템 문서)
- `attachments/` (decorations 이동 후 빈 폴더)

---

## 📋 확정된 결정 (이번 세션)

### 1. A형 상단 슬롯 좌표 ([[1-2-1. 레이아웃 규칙]] §3.1)
| 슬롯 | 위치 |
|------|------|
| 상단형 (1:1266 / 1:1269) | `x353 / y46 / w217 / h457` ⭐ (y68→y46) |
| 하단형 (1:1251 / 1:1246) | `x353 / y56 / w217 / h457` |

### 2. 이미지 자산 도메인 응집
- `decorations/` 외부 자산은 **블로그 도메인 안**에 (`1-3-3. 이미지 자산/데코/`)
- 이미지 규칙·출처·실물 PNG가 같은 1-3 폴더에 모두 모임
- 코드/도구 산출물은 별개 (`자동화 도구/cdbd-capture/screenshots/`에 유지)

### 3. `local_path` 의미 명확화 (`decorations.json` `_format`)
- ❌ 자동화 입력 (figma_hash가 source)
- ✅ vault 백업 (재업로드·복구·문서 임베드용)
- → `magic-wand` 패턴 (PNG 없어도 figma_component_id로 동작)이 이미 검증함

### 4. intake 패턴 통합 ([[0. 목표와 비전]] §파이프라인 1)
- 옛 `1-4. 프롬프트.md` §1 intake 텍스트 → 0의 §파이프라인 1단계에 inline 박스
- §2 변수 명시 템플릿 + 사용 예시는 **자동화 도래로 의미 상실** → 삭제

### 5. "현재 구조의 한계" 섹션 신규 ([[0. 목표와 비전]] 하단)
- 왜 Claude가 use_figma로 바로 안 만드나 (호출 수·속도·트랜잭션·토큰 비교)
- 진짜 자동화의 마지막 장벽 (plugin 외부 트리거 불가)
- 가능한 미래 경로 3가지 (use_figma 직접 / 외부 트리거 / 현 구조 + 적용 확장)

---

## 🎯 다음 세션 우선순위 (이전과 동일)

### P1. ✅ 검증 완료
- ~~D 유형 envelope 테스트 결과 확인~~ → **사용자 OK**

### P2. 자동화 확장 (메인 다음 목표)
- [ ] **B 유형** (목업+에디터) — VISUAL_SLOT + EDITOR_SLOT 동시 처리. Playwright 캡쳐 인프라(`자동화 도구/cdbd-capture/`) 이미 구축됨
- [ ] **C 유형** (멀티 목업) — VISUAL_SLOT_1/2/3
- [ ] **E 유형** (배경) — BG_SLOT opacity 20%

### P3. 자산 라이브러리 확장
- [ ] §5.4 needed 5종: card·book·chat-bubble·pen·chart-up
  - "card"는 thiings 슬러그 탐색 필요
- [ ] 보조 데코 컴포넌트 라이브러리에 envelope 등 추가

### P4. 베리에이션 워크플로우 검증
- [ ] §1 다층 분석 + §5.4 매핑을 5-10개 타이틀로 검증

### P5. (선택) `cdbd-capture/screenshots/`도 .gitignore 추가
- 현재 README는 "(git 무시)"라고 표기하지만 `.gitignore`에 빠짐 (`-raw`만 있음)
- 의도와 실제 일치시키려면 추가 필요

---

## ⚠️ 알려진 이슈·결정 대기 (이전 세션에서 인계)

1. **마스터 75:34 베젤 5px 처리**: 사용자가 직접 등록함
2. **컴포넌트 변환·section 이동은 수동**: Figma MCP가 commit 안 함
3. **`throw new Error()`로 결과 출력 시 롤백**: 변경/검증 분리 필요
4. **인스턴스 자식 fill은 override 가능 / 위치·크기는 불가**
5. **`master.resize()`는 자식 위치 anchor 부작용** → 자식 위치 재정렬 필수
6. **`instance.rescale()`은 자식 비례 변환**

### 이번 세션 추가
7. **공백 있는 한글 경로**: bash 명령 시 따옴표 필수
   ```bash
   python3 "자동화 도구/cdbd-thumbnail-plugin/auto-fetch-decoration.py" list
   cd "자동화 도구/cdbd-capture"
   ```
8. **옛 컨텍스트 노트는 갱신 안 함**: 강현이-2026-05-26·29.md에 `scripts/cdbd-...` 경로 남아있지만 작성 시점 사실 기록이라 보존

---

## 🚀 빠른 시작 (다음 세션 첫 작업 시)

1. 이 문서 + [[0. 목표와 비전]] 읽기
2. 작업 시작 전 현황 빠른 확인:
   ```bash
   python3 "자동화 도구/cdbd-thumbnail-plugin/auto-fetch-decoration.py" list
   ls "블로그/썸네일/1. 디자인 가이드/"
   ```
3. Figma 파일 상태는 26:2 페이지 확인 (이전 D 유형 envelope 안1~4 보존됨)
4. **다음 우선순위 P2 (B 유형 자동화)** 또는 P3 (자산 확장)부터

---

## 📝 이번 세션 12 tasks 요약

| # | 작업 | 결과 |
|---|------|------|
| 1 | A형 상단 슬롯 y68→y46 반영 (1-2-1 §3.1) | ✅ |
| 2 | `1-4. 프롬프트.md` 분석 → 자동화로 의미 상실 판단 | ✅ |
| 3 | intake 패턴 → [[0. 목표와 비전]] §파이프라인 1로 inline 통합 | ✅ |
| 4 | `1-4. 프롬프트.md` 삭제 + 1-3-1/1-3-2 백링크 갱신 | ✅ |
| 5 | `블로그/썸네일/attachments/` 17개 orphan PNG 제거 (1.2MB) | ✅ |
| 6 | `attachments/captures/` 18개 시행착오 PNG 제거 (11MB) | ✅ |
| 7 | `/screenshots/` vault 루트 orphan 제거 | ✅ |
| 8 | `1. 블로그/` 옛 디자인 시스템 폴더 제거 (2026-05-22) | ✅ |
| 9 | "현재 구조의 한계" 섹션 0에 추가 (왜 플러그인 단계) | ✅ |
| 10 | `attachments/decorations/` → `1-3-3. 이미지 자산/decorations/` 이동 + local_path 7개 + 문서 6곳 갱신 | ✅ |
| 11 | `scripts/` → `자동화 도구/` rename + 모든 참조 일괄 갱신 (gitignore, README, 0/1-3-1/1-3-2, 강현이-31 인계) | ✅ |
| 12 | `decorations/` → `데코/` rename + 코드/JSON/문서 일괄 갱신 + `auto-fetch-decoration.py list` 동작 검증 | ✅ |

---

**Last Updated**: 2026-05-31 (2번째 세션 종료 시점)
**Session Focus**: vault 구조 정리 (자동화 코드 변경 거의 없음)
**다음 세션 첫 액션**: P2 (B 유형 자동화) 또는 P3 (자산 라이브러리 확장) 중 선택
