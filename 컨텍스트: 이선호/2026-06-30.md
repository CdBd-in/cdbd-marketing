# 세션 컨텍스트 — 2026-06-30 윅스 임시저장 확인 + 주제 분류 가이드 폐지(캡션 기반 전환) + 1-2 본문 전반 정리 + (재개) 본문 이미지 신기준 반영·구 캡처 인프라 폐지 + (3차) 페이지 히스토리 본문 이미지 6컷 실제 제작

> 이 문서는 새 대화에서 작업을 바로 이어갈 수 있도록 작성된 컨텍스트 파일이다.
> 이전 세션: [[2026-06-24]]
> 🔁 **재개 세션 있음** — 같은 날 두 번째 작업(본문 이미지 "신규 템플릿" 기준 반영)은 아래 **「완료한 작업 (2026-06-30 재개 세션)」** 참조.

오늘 핵심: ① **윅스 블로그 임시저장(드래프트) 조회**(Wix MCP) → ② 7번 글 본문에서 **`*캡션*` 마커 구조** 확인 → ③ "캡션이 있으면 **글 전체 주제 분류가 불필요**" 결론 → ④ **`주제 분류 가이드` 문서 폐지**(살릴 내용 이관, 파이프라인 캡션 기반 6단계로 재편) → ⑤ **1-2 본문 폴더 전반 정리**(구 기준·중복·구 슬롯명·구 레터 일소).

---

## ⚠️ 폴더 구조 변경 (최우선 인지)

세션 중 사용자/린터가 **1-2 본문 문서를 재번호**했다. **다음 세션은 새 경로/이름만 사용.**

```
1. 블로그/1-2. 본문/
├─ 1-2-1. 이미지 템플릿/
│   1-2-1-1. 공통 · 1-2-1-2. 기본 · 1-2-1-3. 강조 · 1-2-1-4. 비교 · 1-2-1-5. 나열 · 1-2-1-6. 프로세스
├─ 1-2-2. 신규 기획 가이드/
│   1-2-2-1. 목표와 비전 · 1-2-2-2. 이미지 제작 가이드 · 1-2-2-3. 텍스트 작성 가이드
└─ 1-2-3. 검수 체크리스트
```

| 구 이름 | → 신 이름 |
|---|---|
| 1-2-1. 목표와 비전 | **1-2-2-1. 목표와 비전** |
| 1-2-3. 이미지 제작 가이드 | **1-2-2-2. 이미지 제작 가이드** |
| 1-2-4. 텍스트 작성 가이드 | **1-2-2-3. 텍스트 작성 가이드** |
| ~~1-2-2. 주제 분류 가이드~~ | **🗑️ 삭제됨** (이번 세션 폐지) |

- **위키링크는 린터가 짧은 형식(`[[1-2-2-2. 이미지 제작 가이드]]`)으로 자동 정규화** → 파일명 기준 해석. (편집 중 파일이 바뀌면 Edit 전 재-Read 필요 — 이번에도 한 번 발생.)

---

## 작업 파일

볼트: `cdbd-marketing`. 모든 경로 상대.

- [[1-2-2-1. 목표와 비전]] — 자동화 파이프라인 **캡션 기반 6단계로 전면 재작성**
- [[1-2-2-2. 이미지 제작 가이드]] — §5 빌드 플랜·§6 트랙 신설 + 구 슬롯명/v2/구 레터 일소
- [[1-2-2-3. 텍스트 작성 가이드]] — §2.5 어휘 사전 보편어 교체
- [[1-2-1-1. 공통]] — breadcrumb 갱신 + §5 중복 tombstone 삭제
- [[1-2-3. 검수 체크리스트]] — TOP#1(폐기 노드) 제거·재번호
- 🗑️ **삭제**: `1. 블로그/1-2. 본문/1-2-2. 신규 기획 가이드/1-2-2. 주제 분류 가이드.md` (`git rm`)

---

## 오늘 완료한 작업 (2026-06-30)

### 1. 윅스 블로그 임시저장(드래프트) 조회 — Wix MCP
- 연결된 사이트 6개 중 **CdBd**(`4667b508-705f-4c74-a3f0-a75613760afa`)의 드래프트 조회.
- **임시저장 7건**(모두 `status=UNPUBLISHED`, `AUTO_SAVE`) 확인:
  1. **추석 시리즈 6편** (`#1~#6.png` 대표이미지, 식품 제조사 추석 카탈로그) — 소개·사용법·비교·비용효과 골고루.
  2. **씨디비디 페이지 히스토리** (7번, 기능 소개, 대표이미지 없음) — 본문 분석 대상.
- API: `POST /blog/v3/draft-posts/query`(filter `status:UNPUBLISHED`) → 목록 / `GET /blog/v3/draft-posts/{id}?fieldsets=RICH_CONTENT` → 본문.

### 2. 7번 글 본문 구조 발견 — `*캡션*` 마커
- 본문(richContent)에서 **`*…*` 로 감싼 PARAGRAPH 줄 = 이미지 자리 + 그 자리의 캡션(내용 설명)**.
- 글 끝 "⚠️ 편집 메모"에 **플레이스홀더 6개**(신규 제작 3 · 기존 스크린샷 3) 현황표가 박혀 있음(발행 전 삭제용).
- → **위치·개수·내용이 캡션으로 이미 결정**되어 있다는 점이 이후 모든 판단의 근거.

### 3. 개념 결론 — "글 전체 주제 분류, 꼭 필요한가?" → 제작 단계엔 불필요
- 사용자: **"캡션이 없는 상황은 없을 것"** → 글을 장르로 분류해 이미지 수를 추론할 필요가 사라짐.
- 5개 목적(기본·강조·비교·나열·프로세스)은 원래 **이미지 1장 단위**라 캡션만 읽으면 결정됨.
- 글 단위 1장르 강제는 오히려 자리별 목적 차이를 뭉갬 → **올바른 단위 = 캡션(이미지 자리)**.

### 4. `주제 분류 가이드` 폐지 + 살릴 내용 이관
의존성 확인(목표와비전 ~12곳·공통·이미지제작·텍스트 참조) 후 다음과 같이 분해:

| 원본 섹션 | 처리 |
|---|---|
| §0 분류 축·§1 분류기·§1.1 규칙·§2·§2.1 | **삭제** (캡션이 대체) |
| §2.2 빌드 플랜(+출력 스키마) | → **이미지 제작 §5**(캡션→목적·템플릿·트랙) |
| §3 배치 순서 | **폐기** (캡션 위치가 곧 배치, clone→한 줄 정렬이 처리) |
| §4 제작 트랙 | → **이미지 제작 §6** |
| §5 자동화 흐름 | → **목표와 비전**에 반영(중복 복사 없이) |

**파이프라인 재편**(목표와 비전):
구 ①주제분류→②세트→③빌드→④트랙→⑤자산→⑥합성→⑦검수 (7단계)
→ 신 **①캡션 추출 → ②빌드 플랜 → ③트랙 분기 → ④자산 확보 → ⑤Figma 합성 → ⑥검수** (6단계)

### 5. 1-2 본문 폴더 전반 정리 (구 기준·중복·잔재 일소)
- **이미지 제작 가이드**: §0 "v2 기준" 슬롯표(`SCREEN_*`·`EDITOR_CANVAS`·`ASSET`·`DECO_ICON`) + 내장부품 tombstone(`1:10590`·`1:10838`·`BG_CANVAS`) 삭제 → **clone 교체 레이어 기준**으로 재작성. 구 레이아웃 레터(`D·E`·`A·C·F·G`·`N·O·R`) 전부 평문/목적명으로 교체.
- **공통 §5 "폐지된 구 체계"** 삭제 (intro와 내용 중복).
- **텍스트 §2.5 어휘 사전**: `소트리더십→인사이트`·`라운드업→모음·추천`·`가이드→사용법`·`비용/ROI→비용·효과`·`기능 출시→소개·출시`, `프로필링크`는 (소재) 표기. "주제"→"글 성격".
- **검수 TOP#1**(`멀티페이지 목업 98:2643` + `Change-This` 채움 = clone 모델로 폐기된 워크플로) 제거 → **TOP 7 → TOP 6 재번호**.
- 편집 문서 `updated: 2026-06-30` 정합.

**검증**: 구 기준 토큰 0 · 본문 내 깨진 wikilink 0(정규식 오탐 `[[1,0,0]]`=코드).

---

## 완료한 작업 (2026-06-30 재개 세션) — 본문 이미지 "신규 템플릿" 기준 반영 + 구 캡처 인프라 폐지

> 사용자가 **오늘 새로 기준 세운 본문 이미지 템플릿 규칙**(목업·디자인보드·모달·버튼·카드리스트·3D·비교 + 더미데이터 규칙)을 전달 → 문서에 반영. 충돌하는 이전 기준은 **새 규칙 우선**으로 교체. 이어서 **옛 viewer 슬러그 Playwright 캡처 인프라 문서를 완전 폐지**.

### 작업 파일 (재개 세션)
- [[1-2-2-2. 이미지 제작 가이드]] — 상단 글로벌 규칙·**§2 목업 소싱 전면 교체**·§3 에디터 소싱 축약·**§7 요소별 제작 규칙 신설**·구 §2.1~2.3/구 §3 삭제
- [[1-2-3. 검수 체크리스트]] — TOP `0-b`(피그마 화면 더미→가상데이터) 신설
- [[1-2-1-3. 강조]] · [[1-2-1-4. 비교]] · [[1-2-1-5. 나열]] — §7 포인터 + 키워드 컴포넌트 교체 연결
- [[1-2-1-2. 기본]] · [[1-2-2-1. 목표와 비전]] — `viewer` → 목업 소싱 표현 정리

### A. 글로벌 규칙 신설 — 피그마 화면 = 더미 텍스트 → 가상 데이터 매핑
- 실제 브라우저 스크린샷이 **아닌** 피그마 화면(목업·디자인보드·모달·카드리스트·디자인 파일)을 쓰면, 더미/플레이스홀더(`YYYY.MM.DD`·`홍길동`·`₩0` 등) 확인 → **일관된 가상 데이터셋 매핑** 후 이미지화. (매핑은 `/figma-dummy-data` 스킬)
- 실데이터(공개 viewer/브라우저 직접 캡처)는 예외. 위치: [[1-2-2-2. 이미지 제작 가이드]] 상단 콜아웃 + [[1-2-3. 검수 체크리스트]] TOP `0-b`.

### B. 목업 소싱 신기준 (§2 전면 교체)
| 구분 | 예시 노드 | 규칙 |
|---|---|---|
| 모바일 목업 (§2.0a) | `358:2090` | "phone" 그룹 오토레이아웃, 스크린샷 width=fill. **원페이지 align=top / 멀티페이지 align=center**. 기본 캡처 = 페이지 시작(원페이지 최상단 / 멀티 1페이지). 출처 = `cdbd.in/templates/{slug}` 우측 목업 미리보기(개발자도구 `border-radius:0`·`border:none` 후 내부 영역만) |
| 데스크탑 목업 (§2.0b) | `358:2078` | "desktop" 그룹, 원본 비율 **가로 1650**. 출처 = 템플릿 상세 / 디자인 파일(전체화면 `ZSRFVPLU8wl86ZBvpccgYA` · 권한관리 `ElnOUBxHSbCBw8ON5hqiST` · 멀티페이지 `6gBlRMcRxk49pMrRteDkb5`)에서 주석·마커 삭제 |

### C. 요소별 제작 규칙 6종 신설 (§7)
> 🔴 공통 — **스케일 먼저 적용 → border-radius·그림자 등 디자인 속성 나중 재적용** (스케일이 속성값을 깨므로).

| 요소 | 예시 노드 | 소스 / 절차 |
|---|---|---|
| 디자인 보드 | `377:5683` | DS `14880:17677` 메뉴 프레임 → 스케일 **1.1배** → 모서리·그림자 |
| 모달 | `379:2995`·`373:3146`·`370:2769` | DS `15207:18551` → 스케일 → **border-radius 40** → 그림자 |
| 버튼 강조 | `377:4645` | 예시 그대로 스케일 → **원래 모서리·그림자 재적용 확인** |
| 카드 리스트 | `347:3712`·`356:1550` | 가상 카드명·아이콘 매핑. `badge` `swap icon` → **아이콘 색상 white 오버라이드(필수)** |
| 3D 아이콘 | `377:5073` | 위치 유지·컴포넌트만 키워드 교체, 없으면 thiings → 컴포넌트 셋 추가 |
| 비교(기존문제) | `377:3957` (+비용 `377:4013`) | 키워드 컴포넌트 교체: `paper`(인쇄물 공용)·`catalog`·`namecard`·`invitation` |

- DS 파일 키: `9GM1FagjqiyrQHyHLBGoFj` (CdBd Design System). 위치: [[1-2-2-2. 이미지 제작 가이드]] §7.

### D. 옛 viewer Playwright 캡처 인프라 폐지 (사용자 결정)
- **AskUserQuestion 2건 결정**: ① 삭제 범위 = **"캡처/소싱 방법만 교체"**(5목적 라우팅·`377:5848` clone·1-2-1-x 문서 유지) / ② viewer 슬러그 Playwright 인프라 = **"완전 삭제"**.
- 삭제: 구 §2.1~2.3(`capture-seminar-3sections.mjs`·`SECTION_HINTS`·`forceY`·thum.io·슬러그 fallback) + 구 §3(admin 로그인 캡처) → **멀티 목업 복붙금지 원칙·에디터 소싱은 신방식으로 압축 보존**(새 §2.1 / 새 §3).
- 참조 정리: §0 표·§5.1·§6 트랙·빌드 스키마·TODO + [[1-2-1-2. 기본]]·[[1-2-1-5. 나열]]·[[1-2-3. 검수 체크리스트]]·[[1-2-2-1. 목표와 비전]]. 잔존 `viewer/Playwright` 표현은 **폐기 명시·실데이터 예외·카피 금칙어 예시**뿐(의도).

### 이번 재개 세션 추가 결정사항
- **피그마 화면 = 더미 → 가상 데이터 매핑 전역 필수** (공개 viewer/브라우저 직접 캡처만 예외).
- **목업 기본 소스 = `cdbd.in/templates` 미리보기**(모바일) · **디자인 파일 가로 1650**(데스크탑). 옛 viewer 슬러그 캡처는 폐기.
- **요소 제작은 §7 우선** — 목적 문서(강조·비교·나열)와 충돌 시 §7. **스케일 먼저, 디자인 속성 나중.**

---

## 완료한 작업 (2026-06-30 3차 세션) — 페이지 히스토리 본문 이미지 6컷 실제 제작

> 7번 글(씨디비디 페이지 히스토리, 드래프트 `98847642-…`)의 본문 `*캡션*` 6개에 대해 이미지 6컷(C1~C6)을 실제 제작. **여러 차례 사용자 피드백으로 전면 재작업** → 최종 기준 확립. 신규 사실(템플릿 섹션 이동·라이브러리·DS 모달 import·실제 화면 소스) 다수 확보 → 문서 반영.

### 산출물 — 출력 페이지 `💻 AI 블로그 이미지 제작` (`1:20182`), 한 줄(y=12000)
| 컷 | 캡션(요약) | 최종 노드 | 유형 / 구성 |
|---|---|---|---|
| C1 | 쌓이는 버전 히스토리·되돌리기 | `396:1641` | ②강조 버전복구 액션 (템플릿 `383:5882` clone, 모달·커넥터 제거) |
| C2 | 버전별 게시 시각·작성자 | `416:1721` | ①기본 크롭 (`383:6957`, 헤더+4~5행 확대·우측 크롭 X) |
| C3 | 복구 클릭 시 확인 단계 | `396:1657` | DS 모달 import + `rescale` (에디터 버전 복구 확인 모달) |
| C4 | 청첩장 문구·장소 버전 누적 | `422:1692` | ④나열 3단(`383:5912`) 같은 페이지 3버전+템플릿배지+변경강조 |
| C5 | 명함 직함·가격·구성 누적 | `423:1663` | ④나열 3단 동일 (직함/구성 수정 강조) |
| C6 | 에디터 vs 데이터 히스토리 별도 | `436:1638` | 두 화면 대각선·겹침 (에디터+데이터 히스토리) |

### 핵심 신규 사실 (다음 세션 필수)
- 🔴 **「템플릿」 섹션 이동: `377:5848` → `383:5681`** (사용자가 이동). 모든 템플릿 노드 ID가 `383:*`로 갱신 — 매핑은 [[1-2-1-1. 공통]] §3.
  - 3단 `383:5912` · 2단 `383:5941` · 모바일+데스크탑 1/2/3 `383:6634/6585/6536` · 버튼강조 `383:5907` · **버전복구모달(=버튼+모달강조 L) `383:5882`** · 수정사항게시(S) `383:6819` · 개인화데이터 `383:5925` · 키워드하단 `383:5699` · 비용저울 `383:6415` · 카테고라이징 `383:6048` · 그리드 `383:6354` · 사용법 `383:5752` · **기본크롭(제출된답변 S) `383:6957`** · 키워드상단배지/**텍스트배지 `383:5740`** `383:5722`. 자산(Problem `377:3936`·Expensive `377:4011`·Templates `377:4999`·3d Icons `377:5070`)은 ID 유지.
- 🔴 **재사용 라이브러리 `383:8608`("모달/페이지 활용")** — 한 번 쓴 화면/모달을 키워드명 프레임으로 추가→재사용. 보유: **에디터 버전 히스토리 `383:7722`**(1650×1080, 편집 텍스트 `Label text`+폰 스크린샷 2슬롯) · 데이터 관리 `383:7482` · 제출된 답변 `383:7461` · Modal/Alert/Created URL `383:8470` · Modal/Confirm/Icon `383:8578`.
- 🔴 **버전/데이터 히스토리 실제 화면 = 권한관리 파일 `ElnOUBxHSbCBw8ON5hqiST` "👨🏻‍💻 개발 문서" 페이지(`306:44007`)**:
  - 버전 히스토리 `5104:18847`(목록+폰, 1920×1080) · 1024 레이아웃 `5104:20265` · 패널만 `5055:30684`
  - 데이터 버전 히스토리 `5104:20329`(목록+표, clean)
  - 복구 확인 모달: 에디터 `5104:20029` · 데이터 `5104:19771` · 완료 `5104:20109`/`5104:19948`
  - 모두 더미(`YYYY.MM.DD`·`id@email.com (홍길동)`·`페이지 제목 1`·표 `Text`) → 주제별 가상데이터 매핑 후 캡처.
- 🔴 **모달은 DS Feedback 페이지에서 키 import** (권한관리 모달은 detached 프레임 → cross-file clone 불가): DS `9GM1FagjqiyrQHyHLBGoFj` → **"Feedback" 페이지 `13957:18220`**. `Modal/Confirm/Text` Desktop key **`529f108261d246dc61050820bef96d0d9d5255d4`**(제목+본문+취소/확인). `importComponentByKeyAsync(key)`→`createInstance()`→더미 `Text` 채움→`rescale(K)`. (set key `3cfac6c3…` · Published URL `713937f0…` · First URL `40fc500f…`)

### 확립된 제작 규칙 (반복 피드백으로 확정)
- **임의로 그리지 말 것** — 본문 이미지는 실제 템플릿 clone. 프레임 spec **bg `#EFF0F0`(0.937,0.941,0.941)·radius 40** (이전 #F5F5F7/24는 틀림).
- **서비스 화면 임의 제작 금지** — 버전/데이터 히스토리는 실제 화면(개발 문서) 캡처. "문구 변경" 같은 화면으로 확인 불가한 허위 내용 ❌.
- **더미→가상데이터 매핑** — 캡처 전 매핑. 사용한 데이터셋: The Heritage Summit/THE SAGE/정은서·이현석 · A1 Security/김우성·한지혜 · 이서연♥김도현 청첩장.
- **모바일 목업 = phone 그룹 복제** + 원페이지면 **템플릿 스크린샷 폴더 `/Users/designer/Desktop/CdBd/마케팅/CdBd 템플릿 스크린샷/{slug}/01~NN.png`를 순서대로 세로 합성**해 채움(1장 반복 ❌). 멀티슬롯은 합성본 CROP 분할.
- **dev-doc 화면 캡처 = `get_screenshot ... contentsOnly:true`**(겹친 형제 주석 마커 제외) + 화면 내부 박힌 파란 주석은 blue stroke 노드 찾아 `remove()`.
- **모달 = DS 컴포넌트 import 후 `rescale`** (래스터 ❌, 외곽 여백 충분, 버튼 텍스트 비율 유지). `node.rescale(factor)` 사용 가능.
- **기본형 가독성** — 확대해 텍스트 읽히게, 목록 헤더+4~5개, 행 우측 크롭 ❌. 허전하면 3D 아이콘 보조.
- **"바뀔 때마다 쌓인다" = ④나열**(같은 페이지 N버전 + 상단 **템플릿 텍스트 배지** + 변경부 강조 박스). **"별도/대비" = 두 화면 대각선·겹침**(작게 나열 ❌).
- **섹션 맥락으로 유형 선택** — 캡션 문구만 보지 말고 이미지 전후 글·섹션 핵심 파악.

### 문서 갱신
- [[1-2-1-1. 공통]] §0·§2·§3 노드 인덱스 전체 `383:*` 갱신 + 이동 안내.
- [[1-2-1-3. 강조]] §0/§2/§3/§4 + §5 버전복구모달 내부 레이어(`383:5883`~) 갱신.
- [[1-2-1-2. 기본]]·[[1-2-1-4. 비교]]·[[1-2-1-5. 나열]]·[[1-2-1-6. 프로세스]] 링크 갱신 + 이동 배너.
- [[1-2-2-2. 이미지 제작 가이드]] **§0.5 "모달/페이지 활용 라이브러리" 신설**(라이브러리 목록·캡처 규칙·모달 DS import 키·나열/두화면/섹션맥락) + §2.0b 개발 문서 화면 소스 + clone 소스 `383:5681` + §5.1 ID 갱신.

### ⚠️ 사고 + 복구 (반복 절대 금지)
- 정리 스크립트에서 **`x<=-4000` 좌표 조건으로 무관한 기존 프레임 13개를 잘못 삭제**(A INHA·VIP-B1·ROI-C6·#1~#6 등) → 사용자가 Figma **`Cmd+Z`로 복구**.
- **교훈: `use_figma` 삭제는 좌표·이름 휴리스틱 금지, 반드시 명시한 노드 ID로만.** `currentPage.children`을 좌표로 필터링해 remove 하지 말 것.

---

## 핵심 설계 결정사항 (다음 세션이 따라야 할 새 기준)

- **캡션이 진실의 원천**: CdBd 블로그 본문엔 이미지 자리마다 `*캡션*`이 박혀 있다. **글을 장르로 분류하거나 이미지 수를 추론하지 않는다** — 캡션 1개 = 이미지 1장, 위치·개수·내용은 캡션이 결정. 캡션 없는 자리엔 이미지 안 만듦.
- **목적 5종은 per-image** (기본·강조·비교·나열·프로세스). 캡션 메시지 → 목적 1개 결정은 [[1-2-1-1. 공통]] §1 라우팅, 캡션→목적·템플릿·트랙 매핑은 [[1-2-2-2. 이미지 제작 가이드]] §5.
- **제작 = clone 모델**: 단일 출처 「템플릿」 `377:5848` clone → **교체 레이어(화면 스크린샷 rect·3D instance·텍스트)만 fill**, 베젤·BG·그리드·화살표·데코는 내장 상속. 레이어는 **타입+위치로 식별**(의미론적 슬롯명 없음).
- **트랙 3종**: 합성(5목적) / 캡처(외부·경쟁사) / 소싱(실사진). 실사진·외부캡처는 자동 합성 불가.
- **주제 분류는 영구 폐기** — 단, "글 성격별 어휘"는 텍스트 §2.5에 보편어로 잔존(분류용 아님, 단어 선택 일관성용).

---

## 윅스(Wix) 접근 정보

| 항목 | 값 |
|---|---|
| 도구 | Wix MCP (`ListWixSites` / `CallWixSiteAPI` / `SearchWixAPISpec`) |
| CdBd 사이트 ID | `4667b508-705f-4c74-a3f0-a75613760afa` |
| 드래프트 목록 | `POST https://www.wixapis.com/blog/v3/draft-posts/query` body `{query:{filter:{status:"UNPUBLISHED"},paging:{limit},sort:[{fieldName:"editedDate",order:"DESC"}]}}` |
| 드래프트 본문 | `GET https://www.wixapis.com/blog/v3/draft-posts/{id}?fieldsets=RICH_CONTENT` |
| 임시저장 7건 작성자 | memberId `d554dcd4-903d-40cd-ae1e-dd9700158449` / category `b9475689-d9e7-40e1-82d2-eb5b59428634` |

**드래프트 ID** (참고): 페이지 히스토리 `98847642-2fe6-4148-86c2-7de905db7874` · 추석 시리즈 `d3923a2c…`(B2B) `755d6d15…`(5분만들기) `a0544af4…`(왜모바일) `8312708c…`(백화점) `1e245db0…`(6이점#3) `4766323a…`(비용#6).

---

## 기술 메모

- **Wix MCP API URL은 대화 컨텍스트에서만** — `SearchWixAPISpec`로 `lightIndex` 검색해 `method.publicUrl` 확보 후 `CallWixSiteAPI`. `body`는 실제 JSON 객체로 전달(문자열 ❌).
- **richContent 파싱**: 본문은 RICH_CONTENT 노드 배열(PARAGRAPH/HEADING/DIVIDER/BLOCKQUOTE/BULLETED_LIST). `*…*` 캡션은 평범한 TEXT라 텍스트 패턴으로 추출.
- **한글 경로 + Edit**: 린터/사용자가 동시 편집 → "File has been modified since read" 발생 가능 → **Edit 전 해당 구간 재-Read**.
- **삭제는 `git rm`** (obsidian-git 자동 커밋). 깨진 링크 검증은 `os.walk`로 파일명 집합 만들고 `[[...]]` 정규식 대조 (jq 없음 → python3).

---

## 미완료 / 향후 확인 필요

- [ ] **검수 TOP#1 삭제 확인** — `멀티페이지 목업 98:2643`(Change-This 채움)을 폐기 판단해 제거함. 본문에서 **가로 스와이프 멀티페이지 목업을 다시 쓸 계획이면 되살려야** 함 (사용자 확인 대기).
- [ ] **강현이 컨텍스트 깨진 링크** — `컨텍스트: 강현이/블로그 이미지 만들기-시작방법.md` L22가 구 분류 체계(비교/사례/라운드업)·`[[1-0. 주제]]`(이전부터 깨짐) 참조. 외부·과거 기록이라 보존했으나, 현 캡션 기반 체계로 갱신할지 미정.
- [ ] **7번 글 대표 썸네일 없음** — 페이지 히스토리 글만 media 비어 있음. 썸네일 제작 가능(`CLAUDE.md` §썸네일 규칙).
- [ ] **추석 시리즈 6편 마커 일관성** — 시리즈 번호가 `#3`·`#6`처럼 들쭉날쭉, hashtags도 `["4편"]`·`["2","6"]` 등 불규칙. 제목·메타·키워드 일관성 검수 여지.
- [ ] **(재개 세션) `자동화 도구/cdbd-capture/` 실제 스크립트 디렉터리 잔존** — 문서상 viewer 캡처는 폐기했으나 **물리 파일은 그대로**. `capture-seminar-3sections.mjs`(viewer·폐기 대상)와 **썸네일/카탈로그용** `capture-catalog-*.mjs`·`에디터 캡처본/`·`login-poc.mjs`가 섞여 있어 임의 삭제 안 함 → viewer 관련만 골라 정리할지 **사용자 확인 대기**.
- [ ] **(3차) C4·C5 변경부 강조 박스 위치 미세조정** — 폰 콘텐츠의 실제 변경 영역에 정확히 맞추는 작업 여지(현재는 대략 중앙).
- [ ] **(3차) C6 좌측 에디터 폰 = The Heritage Summit 페이지** — "이서연♥김도현 청첩장" 제목과 폰 소재 불일치. **웨딩 전용 청첩장 템플릿이 폴더에 없어서** 발생(invitation__seminar/personalized 모두 행사·VIP 초대장). 웨딩 페이지 확보 시 교체.
- [ ] **(3차) 데이터 버전 히스토리를 라이브러리 `383:8608`에 편집형으로 이관** — 현재는 권한관리 `5104:20329` 캡처를 매번 사용(라이브러리엔 "데이터 관리"만 있고 "데이터 버전 히스토리" 없음).
- [ ] **(3차) C2 3D 아이콘(버전 히스토리 상징) 보조** — thiings.co 직접 다운로드는 HTML 반환으로 실패. 자산 라이브러리 `377:5070` 활용 또는 수동 등록 필요(선택).

---

## 다음 세션에서 이어할 수 있는 작업

- ✅ **(완료) 7번 페이지 히스토리 본문 이미지 6컷** — 3차 세션에서 제작 완료(C1~C6, 위 표). 남은 건 미세조정·발행.
- **6컷 발행 연결** — Wix 7번 글 본문의 `*캡션*` 6자리에 C1~C6 이미지 삽입(이미지 업로드→richContent 이미지 노드 교체). 발행 전 본문 끝 "⚠️ 편집 메모" 표 삭제.
- **추석 시리즈 6편 본문 이미지 제작** — 같은 신기준(템플릿 `383:*` clone·라이브러리·더미매핑·나열/비교 규칙) 적용. 식품 카탈로그 소재.
- **검수 TOP#1 / 강현이 링크** 사용자 판단 받아 마무리.
- **추석 시리즈 6편** 메타·키워드·시리즈 마커 일관성 정리(원하면).
